# PROGRESS.md — ZADACHA_GOLQMA.md (всички данни в модела, етапи 1-5)

Работи се без Дака, без рестарт (няма sudo). Всяка стъпка - отделен commit.

## ЕТАП 1 - скоростта

- [x] 1.1 Измерено ПРЕДИ (24.09 ~15:30 UTC), копие на базата, тестов Flask
  (`validation/skorost_20260924.py measure`), числата в `validation/skorost_20260924.csv`
  (етикет ПРЕДИ). /prognozi: 510-640 мс медиана при всеки вариант (празен ден,
  ден с мачове, Приключили, с/без лига) - всеки път чете 26 606 реда, от тях
  25 684 = ЦЕЛИЯТ predictions_log. /prognozi/match/<id>: 2-3 мс (20-25 реда).
  Живият сайт (curl, 127.0.0.1:8001): 0.47-1.05 с. Таблици: predictions_log 25 684
  реда (990 мача), predictions_snapshot 444. Профил: 80% от времето е
  evaluation.published_picks() - вика се ДВА пъти (summary + published) върху целия
  дневник; ~25% е самото четене (SELECT * ... ORDER BY - пълно сканиране + сортиране).
- [x] 1.2 Индекси (EXPLAIN QUERY PLAN): липсваха за status (check_results, /value, уредените),
  match_date в дневника (прозорец за коефициенти), match_date/computed_at в снимката
  (диапазонът по дни беше `substr(match_date,1,10) BETWEEN` - не може да ползва индекс,
  пренаписан като `match_date >= от AND < деня след`). 4 нови индекса в `init_db()`
  на system_tracker.py; вече СЪЗДАДЕНИ и в живата база (init_db тече при всеки импорт).
  След: 425-480 мс - индексите не решават главното (пълното четене + двойното
  published_picks), очаквано.
- [x] 1.3 `list_predictions()` вече приема филтри в SQL (`fixture_ids`, `only_settled_fixtures`,
  `columns`); без аргументи връща точно същото като преди (проверено: 25 684 реда, ==)
  - admin/results/daily/validation го викат без аргументи, непроменени. Нови
  `get_settled_fixture_ids()` и `get_table_version()`. /prognozi: сметките върху целия
  дневник (отчет, "Вчера", "Приключили") се пазят в паметта и се смятат наново само
  при промяна - брояч `table_versions` + 3 тригера върху predictions_log (в `init_db()`;
  ВЕЧЕ създадени в живата база - харнесът ги създаде при първия импорт, преди
  commit-а; безвредни, само броят) + статусите от trust_derived. "Приключили" чете
  редовете/логата само за показаните 25 мача. Резултат: 16-38 мс (беше 510-640).
  Проверка: 100/100 отговора байт по байт еднакви със стария код (7 дни x 5 лиги,
  Приключили по всяка лига и limit, невалидни параметри, 8 мача); след промяна в
  дневника кешът се обновява (3/3 еднакви). ЧАКА РЕСТАРТ за /prognozi;
  system_tracker.py е споделен - самостоятелните таймери го поемат веднага.
- [ ] 1.4 Останалото бавно
- [ ] 1.5 Старо = ново
- [ ] 1.6 Доклад validation/skorost_20260924.md

## ЕТАП 2 - играчите (започнат паралелно, докато 1.4 чака мащабния тест)

- [x] 2.1 `archive/fetch_player_stats.py` -> `fetch_player_stats.py` (корена), пренаписан:
  всички заявки през `api_football._api_get()` (лимит + api_calls.log). Главният CSV
  `{лига}_player_stats.csv` - същите 10 колони, същият ред (player_props.py непроменен).
  НОВО: `{лига}_player_stats_extra.csv` - удари, удари в целта, резерва, id на отбора и
  др. от СЪЩИЯ отговор (старите CSV нямат удари - Етап 4 ги иска).
- [x] 2.2 Таван: общ `backfill_common.py` - спира, щом дневно оставащите заявки
  (заглавката на API-то) паднат под 5000 (= пиков ден на живата система ~3300 +
  1500 запас) -> всички тегления заедно харчат най-много ~2500/ден. 1 заявка/сек,
  пауза при натоварен минутен лимит, до 25 мин на пуск. Продължава от
  `*_progress.txt`. Празен отговор за мач от последните 3 дни - опитва пак по-късно.
  Тест на ръка 24.09 15:36: 3 мача (България - API-то няма играчи за тях, отбелязани).
- [x] 2.3 crontab на inkas: `5,35 * * * *` с flock (лог `fetch_player_stats_log.txt`).
  Опашка при старт: 514 мача догонване (10 лиги, сезон 2026), после 7 втори
  дивизии (5796, Етап 3.1), после удари за старите мачове (6535).
- [ ] 2.4 Проверка след час-два
- [ ] 2.5 Покритие по лига и сезон

## ЕТАП 3 - вторите дивизии и събитията (на фон)

- [x] 3.1 Вторите дивизии - в опашката на същия `fetch_player_stats.py`, сезон 2024+.
- [x] 3.2 `fetch_fixture_events.py` - `/fixtures/events`, CSV `{лига}_events.csv`
  (формат в docstring-а на скрипта и в доклада на Етап 5), всички 17 лиги, сезон 2024+
  (12 850 мача). Тест на ръка: 2 мача, 15:36.
- [x] 3.3 crontab `12,42 * * * *` с flock, същият таван (лог `fetch_fixture_events_log.txt`).
