# Партида 1, т.1.1-1.3: реални API заявки на едно зареждане на /daily?league=all (01.09.2026)

## Метод

Не walk-forward backtest (не пипаме `football_lib.py`/`prediction_policy.py`
тук) - директно преброяване на реални HTTP заявки към API-Football чрез нов
брояч в `api_football._api_get()` (`get_call_count()`/`reset_call_count()`,
единствената точка, през която минават всичките 9 `fetch_*`).

Скрипт (не committed - еднократен, стандартна практика за скрипт, който само
измерва вече живия код, виж `archive/` конвенцията): зарежда
`match_predictor_app.py` директно (без gunicorn), логва се през
`app.test_client()` със сесийна бисквитка (парола от `.env`), рестартира
брояча, прави `GET /daily?league=all` два пъти подред, отчита броя реални
HTTP извиквания на всяко зареждане. Реалната работна база (`predictions.db`)
и реалният API ключ - точно каквото прави истинско зареждане на страницата,
само без Flask/gunicorn HTTP слоя отгоре (in-process WSGI извикване).

## Резултати

| Момент | Зареждане 1 | Зареждане 2 (подред) |
|---|---|---|
| **ПРЕДИ** промяната (git HEAD преди тази партида) | 17 заявки | 17 заявки |
| **СЛЕД** промяната (т.1.1: `use_cache=True, cache_minutes=3` за `_predict_matches_for_league_from_snapshot`) | 17 заявки | **0 заявки** |

17-те заявки на първо зареждане са по една `fetch_upcoming_fixtures()` на
всяка от 17-те активни лиги (`get_leagues()`) - точно списъкът с мачове,
не самите прогнози (те идват от `predictions_snapshot`, вече без API
извикване от по-рано, Граница 2). Не се наблюдаваха допълнителни заявки за
`fetch_lineups_available()` в този конкретен момент - нямаше мачове в
0-60 мин прозореца преди начало в тестовия ден.

**Второто зареждане подред пада до 0 заявки**, точно изискването от
задачата - и 17-те лиги удрят `fixture_list_cache` (споделена таблица,
ключ `(league, from_date, to_date)`), защото първото зареждане вече е
записало резултата там (виж т.1.1 - преди тази партида само фоновите задачи
пишеха в кеша; сега и /daily реално зареждане го пълни).

## Т.1.2, първи опит (01.09.2026, преди преглед) - НЕДОСТАТЪЧНО, вижте поправката веднага след това

> `fetch_lineups_available()` вече минава през `system_tracker.lineups_cache`
> (нова таблица) - веднъж върнало `True`, повече не пита API-то за същия
> fixture_id; `False` изтича след 10 мин. Проверено логически (код преглед +
> `ast.parse`), не с реален мач в 60-мин прозореца - нямаше такъв в момента на
> теста (виж таблицата - 0 допълнителни заявки от тази функция в двата
> случая, защото condition-ът `0<=minutes_to_kickoff<=60` не бе изпълнен за
> нито един от 9-те мача днес).

Дака отбеляза правилно при прегледа: горното НЕ е измерване, само код
преглед. `fetch_lineups_available` е по ЕДНО извикване НА МАЧ - точно този
брой расте с колкото мачове има в прозореца, не 17-те заявки на лига от
таблицата по-горе (тя мери нещо друго - списъка с мачове, не lineups).
Поправка веднага по-долу, със synthетичен fixture и реални API заявки.

**Метод:** синтетичен fixture (fixture_id=999999001, извън всякакъв реален
диапазон), реални имена на отбори от `bulgaria` (has_injuries=False - за да
не се брои и `fetch_fixture_injuries` в резултата, чисто измерване само на
lineups), начален час = сега + 30 мин (вътре в 0-60 мин прозореца).
`match_predictor_app.fetch_upcoming_fixtures` временно подменена (само в
Python процеса на теста, НЕ в живия файл) да връща точно този един fixture.
Извикана е РЕАЛНАТА `_predict_matches_for_league_impl()` (същата функция,
която `/daily` вика) два пъти подред, с `api_football.get_call_count()`
преди/след всяко извикване. Всички редове, писани в `predictions_log`/
`lineups_cache` за синтетичния fixture_id, изтрити веднага след теста -
проверено с директна SQL проверка, 0 остатък.

| Зареждане | Резултат (`lineups_confirmed`) | Реални API заявки |
|---|---|---|
| 1 (студен кеш) | `False` | **1** |
| 2 (подред, същият fixture) | `False` | **0** |

Потвърждава т.1.2 буквално: първото зареждане реално пита API-то (1
заявка), второто удря `lineups_cache` (`False`, TTL 10 мин) и не пита изобщо.

**"True се пази трайно" (другата половина на договора)** - не може да се
провери с реален мач (нямаме такъв с потвърден състав в момента), затова
отделен директен тест на `system_tracker.set_cached_lineups_available(id,
True)` + преместване на `fetched_at` 11 дни назад - `get_cached_lineups_
available()` продължава да връща `True` (не изтича, за разлика от `False`).
Това е тест на самата функция (детерминистичен, не разчита на API
отговор), не на HTTP слоя - честно отбелязано, за да е ясно кое от двете е
"с реална заявка" и кое "с директна проверка на функцията".

## Т.1.3 (червената лента) - проверено отделно

`fetch_upcoming_fixtures()` вече пада обратно към КАКЪВТО И ДА Е кеширан
списък (без TTL проверка) при мрежова грешка/грешка от API-то, вместо да
върне грешка веднага. Тествано с форсирана `RuntimeError` в `_api_get`:

- С предварително кеширан списък (bulgaria, днес) → `err=None`,
  връща кешираните мачове (0 мача в случая, реално число за деня).
- Без НИКАКЪВ кеш за диапазона (изнесена дата 2099) → `err` се връща реален
  ("Мрежова грешка при връзка с API-то: ...") - точно "показвай грешката
  само когато няма какво да се покаже".

## Т.1.1 - приключилите мачове все още се местят коректно

Не наблюдавано на живо (нямаше мач, който да завърши точно по време на
теста, за да се хване преходът "На живо" → "Приключил" в реално време) -
гарантирано по конструкция, не по наблюдение: `cache_minutes=3` означава,
че статусът на всеки мач (включително `status_short`/резултат, идващи от
същия кеширан `fixtures` списък) не може да е по-стар от 3 мин след
изтичане на кеша - точно горната граница, приета от Дака в задачата
("закъснение до 3 мин е приемливо, повече не е"). Класификацията
live/finished/upcoming (`_classify()` в `web/daily.py`) чете директно
`status_short` от същите данни, без отделна логика - няма нов път, по който
да закъснее повече от самия кеш.

## Какво НЕ е пипано

- Фоновите задачи (`run_refresh_odds_cache`/`run_refresh_injuries_cache`/
  `build_predictions_snapshot.py`) - продължават с `cache_minutes=20`
  по подразбиране, споделят СЪЩАТА `fixture_list_cache` таблица, но със
  собствен TTL праг (по-дълъг), без промяна в кода им.
- `_predict_matches_for_league_impl` (`/daily?source=live`, рядка ръчна
  диагностика) - продължава без кеш по подразбиране
  (`use_fixture_cache=False`), непроменено поведение.
