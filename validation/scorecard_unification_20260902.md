# Обединяване на числата на /prognozi (349 vs 310 -> едно число), 02.09.2026

Поискано в `NOSHT3.md`, задача 2. Само публичната `/prognozi` (`web/prognozi.py`,
`templates/prognozi.html`) е пипната. Админ страницата `/results`
(`web/results.py`, `templates/results.html`) остава непроменена нарочно -
Дака иска там пълната картина (349 срещу 310 разделени) за собствения си одит.

## Промяна

1. Нова `evaluation.summary_priced_only(rows, policy, bands=None)` - вариант
   на `summary()`, който смята и четирите показателя (брой, калибрация, ROI,
   печалба) само върху `roi_eligible` (settled публикувани топ-прогнози С
   реален `market_odds`) - същия набор, който `roi()` вече ползва. `summary()`
   самата - непроменена, продължава да захранва `/results`.
2. `web/prognozi.py`: `scorecard = evaluation.summary(...)` ->
   `evaluation.summary_priced_only(...)` - единствената промяна там.
3. `templates/prognozi.html`: нова 4-та плочка "печалба/загуба" в `.metrics`,
   веднага след ROI плочката.

## Проверка n_settled == roi_n по конструкция

Потвърдено реално след деплой (не само по код):

```
NEW n_settled 310 roi_n 310 calib n sum 310
n_settled == roi_n? True
```

## Преди/след, реални числа (02.09.2026, ~20:13 UTC)

| Показател | Преди (`summary()`, стар `/prognozi`) | След (`summary_priced_only()`, нов `/prognozi`) |
|---|---|---|
| брой уредени прогнози | 349 | **310** |
| калибрация | обещахме 63.5% · познахме 65.6% (n=349) | обещахме **62.5%** · познахме **65.5%** (n=**310**) |
| ROI | +0.2% (n=310) | +0.2% (n=310, непроменено - същата формула) |
| печалба/загуба | (нямаше плочка на `/prognozi`) | **+0.6 u** (n=310) - нова плочка |

ROI% самото число не се промени (+0.2%, вече се смяташе само върху n=310) -
промяната е, че сега калибрацията/бройката/печалбата минават през СЪЩОТО
n=310 вместо n=349, плюс печалбата вече се показва на публичната страница.

## /results (админ, непипнат) - проверено да остане непроменен

```
Приключени: 10723 (+7761 чакащи)
Познати (честно): 65.6% (обещано 63.5% · n=349)
Възвръщаемост: +0.2% (на база 310 публикувани със сигурен коеф.)
```

Идентично на преди промяната - `summary()` не е пипната, `web/results.py` не
е пипнат.

## Тест/рестарт

`ast.parse()` на `evaluation.py` - чисто. `kill -HUP` рестарт в 20:13:05 UTC,
`journalctl` чист (само нормалното зареждане на 17 модела от кеша). Реален
curl тест: `/prognozi` (200, четирите плочки показват n=310 еднакво),
`/prognozi/match/<fixture_id>` (200, без грешки), `/results` през
автентикирана сесия (200, старите 349/310 числа непроменени).
