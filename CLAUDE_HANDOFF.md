# CLAUDE_HANDOFF.md — прочети това първо

**Финално чисто резюме към 22.08.2026 (вечер).** Целият оригинален архитектурен план (`ARCHITECTURE.md`, и четирите граници) е затворен. Този файл е пренаписан на тази дата, за да замени дългата стъпка-по-стъпка хроника от 16-22.08.2026 с едно ясно описание на **текущото** състояние — пълната история на КАК се стигна дотук си стои в `git log` (всеки commit описан подробно), не е изгубена, само не е повторена тук дума по дума.

Ти си Claude Code, стартиран директно на живия production сървър на този проект. Нямаш достъп до предишни чат разговори с Дака, нито до claude.ai Project-а, в който е живял първоначалният план (`ACTION_PLAN_v2.md`, `PROJECT_STATE.md`, `ARCHITECTURE.md`) — но самият план вече е изпълнен, затова тези файлове са само исторически контекст, не активна пътна карта.

**Дака (собственикът) е нетехнически.** Разчита изцяло на теб за технически решения. Това означава повишена отговорност: обяснявай простичко, не крий грешки, не бързай с промени по живия сървър без тестване.

---

## 1. Какво е това нещо

Flask приложение за прогнози на футболни резултати, базирано на Poisson/Dixon-Coles модели, обслужвано през gunicorn, зад Cloudflare на домейн `zalog.dd26.eu`.

- Хост: `szalozi`, потребител: `inkas`, работна директория: `~/sportbg-predictor`
- gunicorn: `--workers 1 --threads 4 --timeout 120 --bind 127.0.0.1:8001`, systemd service `match-predictor-app.service`
- SQLite бази: `predictions.db` (прогнози, залози, кеш), `bets.db` (реални залози на Дака) — WAL режим, достъп през `system_tracker.py`/`bets_tracker.py`
- Данни: CSV файлове по лига, `{league}_merged_full.csv` (историческите мачове, върху които се тренира моделът)
- Модели: фитват се лениво при първо извикване, кешират се като pickle в `model_cache/`
- Външен API: API-Football (`v3.football.api-sports.io`) — платен, rate-limited (~7500 заявки/ден)

### Файлова структура (след затварянето на Граница 4)

- `match_predictor_app.py` (1171 реда) — app ядрото: Flask setup, `@app.before_request` auth, регистър на лигите (`ALL_LEAGUES`/`LEAGUE_FLAGS`, ред ~65), модел управление (`get_models`, `compute_grouped_markets`, `_predict_matches_for_league` и цялата Poisson/Dixon-Coles сметка), фонови хелпъри (`run_refresh_all`, `run_refresh_odds_cache`, `run_refresh_injuries_cache`, `run_diagnostics`), регистрация на 5-те blueprint-а по-долу. `/login` живее тук като обикновен `@app.route` (не blueprint — referenced по име от `before_request`).
- `api_football.py` (304 реда) — API-Football HTTP клиент: 9-те `fetch_*` функции + `API_KEY`/`BASE_URL`/`API_HEADERS`/`FINISHED_STATUSES`/`DAYS_AHEAD`.
- `web/` — 5 Flask Blueprint модула, всеки регистриран чрез `register_X_routes(app, ctx)` (ctx dict избягва кръгов import с `match_predictor_app.py`): `results.py` (`/results`), `value.py` (`/value`), `admin.py` (11 админ/диагностични маршрута), `daily.py` (`/`, `/daily`, `/manual`, `/live`), `match.py` (7 маршрута за мач детайли и залози). Endpoint имената са с blueprint префикс (`admin.system_check`, `daily.daily`, `match.match_detail` и т.н.) — важно при `url_for()`.
- `templates/` — 15 `.html` файла (всичкия HTML, преди вграден като Python низове в `match_predictor_app.py`).
- `football_lib.py` — самите Poisson/Dixon-Coles модели (`fit_goals_model`, `fit_goals_direct_covariate`, `dc_adjust_matrix` и т.н.). `prediction_policy.py` — PROVEN/WEAK/REJECTED/UNVERIFIED логика (`TRUST_MATRIX` ръчна + `trust_derived` изведена от реални данни, изведената има предимство щом има достатъчно наблюдения).
- `incremental_refresh.py` — тегли нови завършени мачове + статистика в `{лига}_merged_full.csv` (виж раздел 3 по-долу защо е важен, беше временно счупен).
- Други живи модули: `bets_tracker.py` (`bt`), `system_tracker.py` (`st`), `pick_selection.py` (`ps`), `evaluation.py`, `player_props.py` (`pp`), `bg_names.py`, `production_pipeline.py`, `nightly_snapshot.py`, `build_predictions_snapshot.py`, `build_trust_derived.py`, `refresh_pending_odds.py`, `backup_db.py`.
- `archive/` — еднократни/остарели скриптове (157 преместени в Партида 1). **Внимание:** веднъж се оказа, че архивиран файл всъщност беше жива зависимост (`incremental_refresh.py`, виж раздел 3) — при съмнение дали нещо в `archive/` наистина е мъртво, провери с `grep` дали друг живия файл го import-ва (вкл. лениви import-и вътре в функции, не само на ниво модул).

## 2. Регистър на лигите

`ALL_LEAGUES`/`LEAGUE_FLAGS` в `match_predictor_app.py` (ред ~65) — 17 лиги: bulgaria, england, germany, spain, france, champions_league, europa_league, conference_league, italy, portugal, и вторите дивизии france2/spain2/italy2/portugal2/bulgaria2/england2/germany2.

**Урок, пази предвид:** скриптове за теглене на данни могат да изтеглят CSV за лига, която НЕ е регистрирана в `ALL_LEAGUES` — данните лежат на диска, но никой не ги вижда. Ако Дака каже "изтеглихме X, защо го няма" — първо провери регистъра.

## 3. Текущо състояние — целият оригинален план е затворен

### Архитектура: и четирите граници от ARCHITECTURE.md затворени
1. **Запис срещу експеримент** — `archive/` (157 еднократни скрипта), `validation/` (backtest runner + README, `.gitignore` изключение за `validation/*.csv`), правило в `CLAUDE.md`: промяна във `football_lib.py`/`prediction_policy.py` изисква записан преди/след в `validation/`.
2. **Смятане срещу показване** — нощна/30-минутна фонова задача (`build_predictions_snapshot.py`, `build-predictions-snapshot.timer`) смята прогнозите предварително в таблица `predictions_snapshot`; `/daily` чете оттам вместо да смята на всяка заявка (флаг за връщане `DAILY_USE_SNAPSHOT`, `?source=live` override за сравнение). Старият TTL кеш е премахнат съзнателно ("два кеша, които се борят, са по-лоши от нула").
3. **Измерване срещу правило** — нощна задача (`build_trust_derived.py`, `build-trust-derived.timer`, 06:15) смята PROVEN/WEAK/REJECTED от реални settled данни в таблица `trust_derived`; `prediction_policy.tier()` ѝ дава предимство пред ръчната `TRUST_MATRIX`, щом има достатъчно наблюдения (`MIN_N=20`). Нова лига започва `UNVERIFIED` (показва се, никога не се препоръчва), не тихо `WEAK`.
4. **Логика срещу представяне** — темплейтите излязоха в `templates/*.html`, API клиентът в `api_football.py`, маршрутите по Flask Blueprint-и в `web/` (виж файловата структура по-горе).

### Функционалност, живa и потвърдена работеща
Филтриране на невалидни пазари от показването; каноничен избор на прогноза (`pick_selection.py`) + честни метрики навсякъде, включително ROI/Brier по пазар/лига/седмица (само от ПУБЛИКУВАНИТЕ прогнози, не суровия лог); коефициенти за прозорец от 7 дни напред; предпазител срещу нереалистичен EV (>40% отива в отделен "Пренебрегнати оценки" списък, не в препоръките — `MAX_TRUSTWORTHY_EV`); кеш за контузии/коефициенти/форма-класиране (виж `system_tracker.py`); ръчна бележка + флаг "пропусни мача" на страницата на всеки мач (НИКОГА не пипа `pick_pct`, категорична забрана от Дака); последни 5 мача + позиция в таблицата на `/match_detail`; `/value` показва само PROVEN пазари (по-строго от общото публикуване); нощен бекъп на базите (`db-backup.timer`), нощна проверка на резултатите (`check-results.timer`).

### Известни, безобидни особености (не бъгове за поправка — за информация, ако Дака попита)
- `/match_detail` гърми с 500, ако се извика само с `fixture_id`, без `league`/`home`/`away`/`date` — всички реални линкове в приложението винаги подават и четирите заедно, засяга само хипотетичен стар/непълен bookmark.
- `/diagnostics` понякога показва модел като "остарял/липсва" в кеша веднага след коректно опресняване (виж `update_injuries_for_league()` — презаписва CSV-то безусловно, дори когато нищо не намери, което измества mtime-а след модела) — козметично, самият модел е коректен.
- `refresh_status()` (в `web/admin.py`) съдържа 2 реда мъртъв/недостижим код след `return` (стара референция към несъществуващо `LEAGUES_ADMIN_TEMPLATE`) — никога не се изпълняват, оставени нарочно непипнати при преместването за вярност на "чисто преместване".
- `prediction_policy.market_group()`/`tier()` очакват СУРОВ market_code (напр. `"home_win"`, `"over25"`), не груповото име (`"1x2"`, `"ou25"`) — грешен аргумент тихо връща `"other"`/`WEAK`/`UNVERIFIED` вместо грешка. Лесно за объркване при ръчна диагностика в конзолата.

### Технически капани, пази ги предвид
- **`model_cache/*.pkl` не се преизчислява автоматично**, ако само `football_lib.py` се промени (зарежда се от диска, докато pickle mtime > CSV mtime). Ако пипаш `fit_goals_model()`/`fit_total_model()`/`fit_goals_direct_covariate()`, изчисти `model_cache/*.pkl` преди рестарт.
- **`sudo` не работи в тази сесия** (иска парола, няма терминал) — рестарт чрез `ps aux | grep gunicorn` (най-старият PID е master), после `kill -HUP <PID>`. Ефектът е идентичен на restart. Провери `journalctl -u match-predictor-app --since "<час>"` за "Всички модели са в кеша." и грешки.
- Import alias-и, вече заети: `pp`=`player_props`, `policy`=`prediction_policy`, `st`=`system_tracker`, `bt`=`bets_tracker`, `ps`=`pick_selection`, `evaluation` с пълно име.
- **Ако твърдиш число (backtest резултат, метрика) — файлът с числото трябва да е в git, преди да го твърдиш** (правило в `CLAUDE.md`, виж К.1 инцидента в git история, `project_k1_backtest_regression` в auto-memory, за какво точно се обърка преди това правило да съществува).

### Довършено на 22.08.2026: `incremental_refresh.py` беше архивиран погрешно, вече е върнат

По искане на Дака разследвах защо `/refresh_all` ("Опресни всички данни") тихо не работеше от 21.08.2026. `incremental_refresh.py` тегли нови завършени мачове + пълна статистика в `{лига}_merged_full.csv` — единственото място в системата, което прави това (`build_predictions_snapshot.py` само смята прогнози от вече съществуващи данни, не тегли нищо ново). Файлът е бил погрешно преместен в `archive/` при одита от Партида 1 (класифициран като "еднократен скрипт" по име, докато реално е жива, лениво-зареждана зависимост). Върнат (git commit `a35ea46`), тестван локално, после реално пуснат на живо — **13 нови завършени мача добавени** в 8 лиги (England, Spain, France, France2, Spain2, Italy2, Portugal2, Bulgaria2), моделите презаредени с новите данни (потвърдено и от лога, и от mtime-овете на `model_cache/*.pkl`). Пълните детайли са в `git log` (commit `a35ea46` и обновяващият го commit на `CLAUDE_HANDOFF.md`).

## 4. Работен протокол — препоръка да продължиш да го спазваш

1. **Тествай локално преди всяка промяна.** Пресъздай засегнатия код изолирано, докажи поведението, ПРЕДИ да пипнеш файла на живо.
2. **Патчове чрез програмна замяна с проверка**, не ръчно "по усет" редактиране — точен `replace`/AST-базиран инструмент, синтактична проверка (`ast.parse()`) преди запис. При съмнение за празни редове/дълъг анкер: чети файла директно (`Read`/`repr()`), не разчитай на визуален преглед през relay.
3. **След всяка логическа промяна:** рестарт (`kill -HUP`, виж по-горе), после `journalctl -u match-predictor-app -n 50 | grep -iE "error|traceback"`. Не приемай "изглежда работи" без този резултат — а където е практично, направи и реален функционален тест (реална заявка/API удар/database проверка), не само визуална проверка на страница.
4. **`curl` без сесийна бисквитка връща 302** (login redirect) — за реална проверка на резултат или разчитай на автентикирана сесия (паролата е в `.env`, `LOGIN_PASSWORD=` — `-c cookies.txt -X POST -d "password=$(grep LOGIN_PASSWORD .env | cut -d= -f2)" .../login`), или моли Дака за наблюдение от логнат браузър.
5. **Обяснявай на Дака какво правиш и защо, простичко.** Той не разбира кода, но взима продуктовите решения.
6. **Commit-вай след всяка успешна, потвърдена промяна** (след рестарт + чист `journalctl`), с кратко съобщение какво и защо. `git diff`/`git log`/`git revert` са първото място за проверка при проблем, не паника.
7. `.gitignore` изключва `venv/`, `model_cache/`, `*.db`, `*.csv` (освен `validation/*.csv`), `*.json`, `*.log`, `*.txt`, `*.bak*`, `db_backups/`, `odds_historical/`.

## 5. Сигурност на тайните — извадени от кода 25.08.2026, СТОЙНОСТИТЕ НЕ СА СМЕНЕНИ

**Обновено 25.08.2026 (сесия без Дака, изрично поискано от него):** API-Football ключът, паролата за
вход (`LOGIN_PASSWORD`), refresh token-ът за нощните задачи (`REFRESH_TOKEN`) и Flask session ключът
(`app.secret_key`, открит по пътя — хардкоднат в същия ред, включен по същата логика) вече НЕ живеят в
кода. Всичките четирима живеят в `/home/inkas/sportbg-predictor/.env` (извън git — `.gitignore`
обновен, проверено с `git status`), четени през ново единно `config.py` (chete `.env` по абсолютен
път — работи еднакво за gunicorn/systemd oneshot/cron скрипт, без нужда от промяна в unit файловете).
Трите `.sh` cron скрипта (`refresh_odds_cron.sh`, `incremental_refresh_cron.sh`,
`check_results_cron.sh`) вече `source .env` вместо хардкоднат токен/парола инлайн. Commit `ba2c7fe`.

**ВАЖНО: стойностите НЕ са сменени** — само мястото им. Завъртането на ключа/паролата остава отделно,
бъдещо решение на Дака — виж по-долу "Как Дака сменя стойностите" за точния ред във файла.

**Как Дака сменя стойностите (когато реши):** отвори `/home/inkas/sportbg-predictor/.env` в редактор
(`nano .env`) — четирите реда:
- API ключ: реда, започващ с `API_FOOTBALL_KEY=`
- Парола за вход: реда, започващ с `LOGIN_PASSWORD=`
- Refresh token (нощните задачи): реда, започващ с `REFRESH_TOKEN=`
- Flask session ключ: реда, започващ с `FLASK_SECRET_KEY=`

Смени стойността след `=` (без интервали, без кавички), запази файла, после **рестартирай
приложението** (`kill -HUP <PID на master gunicorn>` — виж раздел 4 по-горе за точния метод), за да
прочете новата стойност — `config.py` чете `.env` еднократно при стартиране на процеса, не следи за
промени на живо.

**Забележка при смяна на `LOGIN_PASSWORD`:** старите сесийни бисквитки на потребителите остават
валидни (не зависят от паролата, само от `FLASK_SECRET_KEY`) — само НОВ вход ще изисква новата парола.

**Открито при тази работа, НЕ поправено (отделен проблем, извън обхвата на извеждането на тайните):**
`check_results_cron.sh` използва HTTP Basic auth (`-u sportbg:...`), който `match_predictor_app.py`
изобщо не проверява никъде (`before_request` проверява само сесийна бисквитка или `X-Refresh-Token` за
3 конкретни `/refresh_*` пътя) — заявките към `/system_check_results`/`/check_results` там вероятно
вече получават 302 (login redirect) вместо реално да проверяват резултатите. Потвърдено с ръчен `curl`
тест — 302 преди И след промяната (поведението не е сменено, само тайната е преместена). Нощната
проверка на резултатите (`check-results.timer`, на всеки 3 часа) може да не работи реално отдавна —
изисква отделно решение от Дака дали да се поправи механизмът (напр. да мине през `X-Refresh-Token`
като другите два cron скрипта, вместо счупен basic auth).

## 6. Финансови/залагания бележки

Дака от време на време пита за мнение по конкретни залози, които вече е направил или обмисля. Отговаряй информативно (изчисления, проверка на съгласуваност между източници), НЕ като финансов съвет, НЕ с уверени препоръки "заложи"/"не залагай". Отбелязвай факти неутрално, без морализиране.

## 7. Ако нещо в този файл противоречи на текущото състояние на кода

Вярвай на кода, не на този файл. Първата стъпка при всяка нова сесия: прочети този файл, после провери реалното състояние (`grep`, `git log`, `systemctl`), и едва тогава действай.

---

## 8. API-Football дневна квота — оптимизация от 22.08.2026 (следобед)

Дака съобщи, че "Предстоящи" (`/daily`) не показва мачове. Причината: дневната
квота на API-Football (7500 заявки/ден) беше напълно изчерпана към ~14:18 UTC
— потвърдено с директна заявка към `/status` на API-то (върна "reached the
request limit for the day"). Не е бъг в кода, а изчерпан външен ресурс.
Началната страница ("Топ прогнози") продължи да работи нормално, защото чете
директно от `predictions_snapshot` (вече изчислена таблица); `/daily` обаче
винаги е викал `fetch_upcoming_fixtures()` **живо** дори в snapshot режим,
само за да разбере СПИСЪКА с мачове (не самите прогнози) — при изчерпана
квота този живо повикване се проваля и списъкът излиза празен, въпреки че
прогнозите за същите мачове реално съществуват на диска.

**Диагностика от реалните логове (`odds_refresh_log.txt`, преди промяната):**
`run_refresh_odds_cache()` (тръгва на всеки 30 мин) сама по себе си
консумираше **~118 `fetch_fixture_odds` извиквания + 17 `fetch_upcoming_
fixtures` (по едно на лига) на всеки цикъл** → ~6480 заявки/ден, ~86% от
целия дневен лимит, преди изобщо да броим контузии/резултати/реални
посещения. Причината: правилото "мач до 24ч преди начало → опресни ако
кешът е >25 мин стар" опресняваше практически на ВСЕКИ единствен 30-мин
цикъл. Отделно, три различни 30-мин фонови задачи (опресняване на
коефициенти, опресняване на контузии, `build_predictions_snapshot.py`)
поотделно питаха API-то за един и същ списък мачове по лига — тройно
повторение на едно и също запитване.

**Промяна, тествана локално (mock тест на кеша + прагове, виж git commit),
после пусната на живо, рестарт чист:**
- `_odds_needs_refresh()` (`match_predictor_app.py`): прагове 25/180/720 мин
  → 90/360/1440 мин (мач до 24ч / 24-72ч / >72ч преди начало).
- Нов споделен TTL кеш (`fixture_list_cache` в `system_tracker.py`,
  20-мин TTL) за списъка с мачове по (лига, период) — ползван САМО от
  трите фонови задачи (`fetch_upcoming_fixtures(..., use_cache=True)`),
  НИКОГА от `/daily`/`/live` при реално отваряне от потребител (там живият
  статус/резултат трябва да е пресен към момента, не до 20 мин стар).
  `run_refresh_injuries_cache()` разшири прозореца на самата ЗАЯВКА до
  same today..+DAYS_AHEAD като odds-опресняването (филтърът до 48ч е
  преместен на Python ниво), за да пада в СЪЩИЯ кеш — тази двойка тръгва
  последователно от един и същ `refresh_odds_cron.sh`, значи попадението в
  кеша е гарантирано. Дедупликацията между `refresh-odds.service` и
  `build-predictions-snapshot.service` (два ОТДЕЛНИ systemd процеса на
  идентичен 30-мин график) е по-несигурна — и двата тръгват на практика в
  същата секунда (виж `systemctl list-timers`), кой ще удари кеша първи не
  е гарантирано; тази част е "може само да помогне, никога да навреди", не
  гарантирана икономия.

**Важно, все още неизмерено:** горните числа за СТАРИЯ разход са реални (от
`odds_refresh_log.txt`). Реалният разход СЛЕД промяната още не е измерен,
защото квотата беше вече изчерпана в момента на пускането — `fetch_
upcoming_fixtures()` връща грешка веднага, преди изобщо да стигне до
опита за кеш/мрежа разликата не личи в логовете. Квотата се нулира
некъде към полунощ UTC (~03:00 ч. българско време). Следваща сесия/при
следващо питане на Дака: провери `odds_refresh_log.txt`/`injuries_
refresh_log.txt` за реалните "проверени"-числа СЛЕД нулирането и сравни с
горните "преди" числа — едва тогава може да се твърди конкретен % спестена
квота (правило от `CLAUDE.md`: число без записан файл в git не е доказано).

---

## 9. Rate limit на минута — поправка от 23.08.2026 (вечер), 4 стъпки

Вечерта на 23.08 `/daily?league=all` пак върна "Too many requests... limit of
requests per minute" — различно от дневната квота (раздел 8). Диагноза: лимитът
на минута е **300 заявки** (Pro план, потвърдено на живо от `/status` header
`x-ratelimit-limit`). Най-прекият виновник: бутоните `/refresh_all` и
`/refresh_odds_cache_manual` стартираха нов фонов thread при ВСЯКО натискане,
без заключване — няколко души по няколко клика = паралелни копия на целия цикъл.
Пълният план е в `PLAN.md` (Фаза −1, писан от Opus 5 успоредно със сесията, която
направи поправката).

Направено, в 4 git commit-нати стъпки (`git log`, търси "rate limit стъпка"):
1. **Заключване** (`_try_start_refresh`/`get_refresh_state` в
   `match_predictor_app.py`) — по ВИД задача (`all`/`odds`/`injuries`), не общо
   за всичко (odds и injuries трябва да могат да вървят едновременно). Видимо в
   интерфейса (банер на `/` и `/leagues_admin`, докато тече опресняване).
2. **Споделен rate limiter** (`_rate_limiter`/`_api_get` в `api_football.py`) —
   sliding-window брояч, таван 280/60s (запас над 300). Нула закъснение под
   прага, изчаква само при доближаване. Всичките 9 `fetch_*` минават през него.
3. **`incremental-refresh.timer`** (нов systemd timer, 04:00 всяка нощ, ПРЕДИ
   05:00 бекъп → 06:00 снимка → 06:15 trust_derived) — вика `/refresh_all` през
   `curl` + `X-Refresh-Token` (по образец на `refresh-odds.timer`), не отделен
   python скрипт — минава през СЪЩОТО заключване като ръчния бутон. Хотфикс в
   същата стъпка: старата auth логика правеше безусловен redirect към `/login`,
   ако токенът липсва — щеше да счупи истинските бутони (те викат `/refresh_all`
   със сесийна бисквитка, без токен).
4. **Разминаване на таймерите** — `refresh-odds.timer`/`build-predictions-
   snapshot.timer` бяха `OnBootSec`/`OnUnitActiveSec` (случайно се засичаха на
   практика в една и съща секунда). Сменени на фиксирани `OnCalendar` (`:00`/
   `:30` срещу `:15`/`:45`) — гарантирани 15 мин разлика завинаги, независимо от
   рестарт на сървъра.

### Важен капан, установен при проверка на живо на стъпка 3 (23.08.2026, ~17:51 UTC)

Дака пусна `incremental-refresh.service` ръчно за тест — `systemctl status`
показа "Finished successfully" за ~5 секунди. **Това НЕ е доказателство, че
опресняването реално е минало.** Причината: `/refresh_all` маршрутът прави
`thread.join(timeout=6)` и връща HTTP 200 веднага щом или (а) заявката получи
заключването и приключи бързо, или (б) hit "вече тече" (busy) — и в двата
случая след най-много 6 секунди, независимо дали истинската фонова задача
(`run_refresh_all()`, обхожда 17 лиги) вече е приключила. За реалните 17 лиги
това отнема от секунди (нищо ново) до минути (много нови завършени мачове +
статистика за всеки). `systemd`/`curl` вижда само "заявката е приета", не
"опресняването е готово".

**Проверка на живо на конкретния случай:** run-ът, стартиран в 17:51:35.031339
(вижда се в `refresh_log.txt`), завърши коректно в рамките на секунди — всичките
17 лиги минати, "Опресняването приключи" + "Загряването приключи" налице,
`journalctl` чист. Но това се потвърди само защото `refresh_log.txt` беше
проверен РЪЧНО след факта — самият "Finished successfully" статус на systemd
не казваше нищо за това.

**Истинската проверка идва от Фаза 1 на `PLAN.md`** ("последно теглене на нови
завършени мачове" с възраст, червено при >24ч) — не от systemd статуса на
юнита. Докато тази проверка не съществува, единственият надежден начин да се
потвърди завършване е да се прочете `refresh_log.txt` за реалния "===
Опресняването приключи ===" маркер СЛЕД времето на стартиране.

### Какво става, ако `match-predictor-app` е спрян/рестартира точно в 04:00

Не е тихо пропускане на systemd ниво — но е тихо за Дака. `incremental_refresh_
cron.sh` е само `curl ... http://127.0.0.1:8001/refresh_all` без `|| true`; ако
портът не приема връзки (спрян/рестартиращ gunicorn), `curl` връща код 7
(потвърдено локално: `curl` към затворен порт → exit 7, `HTTP 000`), скриптът
наследява същия exit code, и `systemd` коректно маркира **`incremental-
refresh.service` като FAILED** (вижда се в `systemctl status`/`journalctl -u
incremental-refresh.service`) — не като "успешно пропуснато".

Но: `Persistent=true` на таймера помага само ако ЦЕЛИЯТ сървър е бил изключен
точно в 04:00 (наваксва на следващото зареждане) — НЕ помага, ако сървърът е
бил включен, но `gunicorn` е имал кратък прозорец точно тогава (напр. `Restart=
always`/`RestartSec=5` след busman crash, или ръчен рестарт). В този случай
задачата просто гърми този единствен път, без автоматичен повторен опит до
следващите 04:00 (или ръчно натискане на бутона). Никой не е известен — няма
табло/аларма за това днес (пак Фаза 1 от `PLAN.md`). На практика прозорецът е
кратък (моделите зареждат от `model_cache/*.pkl`, обичайно 1-2 сек за целия
цикъл), значи шансът за точно засичане с 04:00:00 е малък, но реален и
непроверен автоматично.

---

## 10. Смесено срещу чисто число - решено и приложено навсякъде (нощна сесия 23-24.08.2026)

Продължение на диагнозата от предишната сесия същата нощ (`validation/blend_vs_raw_audit_20260824.md`
т.1-4): твърдението в кода до `BLEND_WEIGHT` ("доказано с бектест на 6 лиги") нямаше файл зад себе
си. Задачите по-долу затвориха въпроса с реален, committed бектест и приложиха резултата навсякъде
по сайта, не само на `/daily`.

### Задача 1: бектест + проверка за шум

`validation/blend_vs_raw_backtest_20260824.py/csv` (475 уредени наблюдения) + нов
`validation/blend_vs_raw_significance_20260824.py/txt` (paired bootstrap, 5000 семпъла, seed=42).
За home_win/draw/away_win/over25/under25 (точно пазарите, които `/daily` реално блендва) разликата
модел/смес **НЕ е шум**: 95% CI изключва нулата и в Brier (0.0065–0.0183), и в log-loss
(0.0143–0.0397). По-слабите точкови оценки по отделни лиги от предишната диагноза
(champions_league, portugal - "моделът сам е по-добър") се оказаха шум при проверка на значимост -
извадките (40 и 45 наблюдения) са твърде малки, за да разграничат посоката. Единствената лига със
самостоятелна значимост е conference_league (56% от извадката, смес по-добра).

**Решение: смесеното число е авторитетно за тези 5 пазара навсякъде по сайта.** Пълните таблици и
разсъждения - `validation/blend_vs_raw_audit_20260824.md`, раздели 5-6.

### Задача 2+3: `/match_detail` и `/value` вече ползват смесеното число

- Нова `_blend_with_market()` в `match_predictor_app.py` (извлечена от `_raw_candidates()`, без
  промяна в поведението на `/daily`).
- `compute_grouped_markets()` вече прилага същото смесване на `home_win`/`draw`/`away_win`/`ou_p`
  веднага след суровата Poisson сметка - каскадира автоматично към главната таблица на
  `/match_detail` И value_bets/EV/Kelly секцията (четат същите променливи). Другите пазари
  (голове по отбор, полувреме/край, корнери, картони, засади) остават чист модел - никога не са
  имали пазарен коефициент.
- `_predict_matches_for_league_impl()` вече подава `real_odds=cached_odds` на
  `compute_grouped_markets()` при логване в `predictions_log` -> `pick_pct` вече е смесен за тези 5
  пазара **занапред** (важи само за новологвани мачове, append-only лог - вече логнати pending
  редове НЕ се пренаписват). `/value` и началната страница четат `pick_pct` директно от лога, значи
  автоматично вече ранкират по смесеното число, без промяна в `web/value.py`.

Потвърдено локално (fixture 1551086: 52.51% смес срещу 59.28% чист модел, +7.1% срещу +20.9% EV -
точно ръчната сметка на Дака) и на живо през `/match_detail`, `/daily`, `/value`, началната страница.

### Задача 4: колко value bets отпаднаха

`validation/value_bets_before_after_20260824.py/csv` - преизчислява СЪЩИТЕ прагове
(`MIN_VALUE_BET_PROB`/`MAX_VALUE_BET_ODDS`/`MAX_TRUSTWORTHY_EV`, непипнати) с чист модел срещу
смесено число, по лига. **506 value + 47 "пренебрегнати" (EV>40%) преди -> 510 value + 7
"пренебрегнати" след.** Общият брой препоръчани залози почти непроменен; блендването основно
смъква преди-недоверените крайности под 40% прага (вече нормални value bets, не предупреждения).
Без систематичен спад по никоя лига.

### Задача 5: картата на `/daily` - максимум 2 залога

Нова `build_pick_card()` избира сред ДОВЕРЕНИТЕ (policy-eligible) кандидати: най-стойностен (най-
висок EV, само сред пазарите с коефициент, същите прагове като value_bets) + най-сигурен (най-
висока вероятност). `top_picks_with_code()` вика се вече с `n=8` вместо `n=3` (за да вижда целия
кандидат-пул, не само топ-3 по вероятност) - `picks_raw[0]` (топ сигурен) остава идентичен на
преди. `templates/daily.html`: до 2 реда вместо 3 чипа, пазарна и наша цена една до друга, дума
"оценка" премахната, изрични текстове при липса на EV/пазарни коефициенти.

### Задача 6+7: `/match_detail` пренареден + различен бутон за непроверени пазари

Само `templates/match_detail.html`, нищо изчислено/премахнато. Value bets най-отгоре (веднага след
очаквани голове/контузии), "Форма и класиране" сгъваема (затворена по подразбиране, нов
`.collapsible`/`.collapsible-head`/`.collapsible-body` CSS модел, копиран от `league-group` в
daily.html), "Твоя бележка за мача" на самия край. Групите Корнери ⚠️/Картони ⚠️/Засади ⚠️ вече
също сгънати по подразбиране (detect по "⚠️" в заглавието). Бутонът "Залог" на тези групи е нов
клас `.bet-btn-unverified` (сив, контурен) вместо `.green` - вече визуално различим от доверените
пазари.

### Всички 7 задачи: commit-нати поотделно, всяка с локален тест + рестарт + чист journalctl +
реален функционален тест през автентикирана сесия. `git log`, търси "Задача 1" до "Задача 6+7" за
пълните commit съобщения с детайли.

### Какво чака решение на Дака

- Нищо блокиращо - всичките 7 задачи затворени, живата система работи с новите числа.
- Забележка за бъдещето: `BLEND_WEIGHT=0.5` е доказан по-добър САМО за home_win/draw/away_win/
  over25/under25 общо (доминирано от conference_league, 56% от извадката) - НЕ индивидуално за
  всяка от останалите 6 проверими лиги (недостатъчна извадка за увереност там). Ако с времето се
  събере повече settled данни по лига, си струва да се пусне `validation/blend_vs_raw_significance_20260824.py`
  наново - възможно е картината по отделни лиги да се изясни.

---

## 11. Етикетите вече не твърдят недоказана печалба (сесия 24.08.2026, продължение)

След раздел 10 (смесеното число) дойде поредица измервания, които промениха извода: **системата
няма доказателство, че "намира стойност"** спрямо пазара. Три отделни проверки, всяка с committed
файл в `validation/`:

1. **`validation/ev_threshold_backtest.py`** (кофи по EV срещу реална ROI, bootstrap значимост) -
   нито една EV кофа между 0% и 40% излиза статистически различима от шум със сегашните данни.
2. **`validation/vs_market_brier.py`** (`brier_vs_market.py`, чист модул, вика се и от живия раздел
   "Бием ли пазара?" в `/results`) - Brier score на нашата вероятност срещу обезвигованата пазарна,
   по лига и пазар, с bootstrap CI и split-half проверка. От 35 комбинации лига×пазар само 5 имат
   n≥30; **нито една не излиза "доказано" или "кандидат"** - `conference_league`/домакин печели е
   статистически **по-зле** от пазара (n=53), другите четири - шум.
3. **`validation/blend_weight_sweep.py`** + **`validation/blend_weight_out_of_sample.py`** -
   претърсване на BLEND_WEIGHT 0.0-1.0 плюс извън-извадкова проверка (първа половина данни търси
   оптимума, втора половина го проверява невиждан). Само `home_win` (оптимум при W=1.0, чист пазар)
   и агрегираното "всички пазари заедно" издържат извън извадката - за `away_win`/`draw`/`over25`/
   `under25` "оптималните" тегла от пълната извадка се оказаха нагаждане към шум, не реален ефект.

**Извод, потвърден от три независими измервания:** никъде няма доказана положителна разлика с
пазара. Дака реши текстовете по сайта да спрат да твърдят обратното. Функционалността (филтри,
прагове, изчисления) **не е пипана** - само думите на екрана:

| Преди | След | Файл |
|---|---|---|
| "НАЙ-СТОЙНОСТЕН" (карта на `/daily`) | "Най-голяма разлика с пазара" | `templates/daily.html` |
| Меню "💰 Стойност" | "💰 Разлики с пазара" | всички 12 `templates/*.html` (sidebar) |
| `/value` заглавие "Стойност" | "Разлики с пазара" | `templates/value.html` |
| "💎 Value bets (EV & препоръчан залог)" (`/match_detail`) | "💎 Разлики с пазара" | `templates/match_detail.html` |
| "за 100 лв. → 107 лв. очаквано в дългосрочен план" | "ако нашата оценка е вярна: за 100 лв. → 107 лв." | `templates/daily.html` |
| "Няма стойност при тези коефициенти" | "Няма разлика с пазара" | `templates/daily.html` |
| "пропусната от пазара стойност" (текст до пренебрегнатите оценки) | "пропусната от пазара възможност" | `templates/match_detail.html` |
| Начална страница: бадж/текст "стойностен" | "разлика с пазара" | `templates/index.html` |
| `/daily` подредба "По стойност" | "По разлика с пазара" | `templates/daily.html` |

Ако с времето `validation/vs_market_brier.py`/`ev_threshold_backtest.py` покажат доказана разлика
(значимо, потвърдено на split-half или нова извадка) - тогава е моментът текстовете да се върнат
към претенция за "стойност"/печалба, не преди това.

---

## 12. Отборни голове (над/под 1.5) и слотът "НАЙ-СИГУРЕН" - три измервания (25.08.2026)

Дака залага основно пазара "отбор над/под 1.5 гола" - три отделни, чисто измервателни задачи (нищо
изчислено/показвано не е пипнато), всяка с committed файл:

**1. `validation/vs_market_brier_team_goals.py`** - разширява `brier_vs_market.py`
(`build_detail_rows_team_goals()`, `TEAM_GOALS_GROUPS`) с home/away над/под 1.5, по същия метод като
`vs_market_brier.py` (24.08.2026). Всяка отделна лига е под `MIN_N=30` (макс. 26 наблюдения) - обединени
по всички лиги: `home_over15`/`home_under15` n=53, `away_over15`/`away_under15` n=53, и двете "шум" (CI
пресича нулата). **Нито доказано по-добро, нито по-зле от пазара - недостатъчно данни за твърдение в
която и да е посока.** Файлове: `vs_market_brier_team_goals_detail_20260825.csv` (212 реда),
`_combos_20260825.csv` (32 реда).

**2. `validation/team_goals_dispersion.py`** - проверка на хипотезата "Poisson подценява опашката, затова
всички 'под 1.5' прогнози са завишени". Walk-forward (13-те `fit_goals_model()` лиги, без
england/germany/spain/france - виж ограничението на `runner.py`), 3694 отбор-мач наблюдения, маргинално
разпределение 0/1/2/3/4+ гола на отбор от СЪЩАТА pm матрица като production. Обединено по всички лиги:
**реално "под 1.5" се случва малко ПО-РЯДКО от очакваното (61.94% срещу очаквани 63.85%, -1.91пр.п.),
"над 1.5" - малко по-често (38.06% срещу 36.15%)** - посоката потвърждава хипотезата (опашката леко
подценена), но ефектът е малък и обединеният хи-квадрат тест НЕ е значим (хи2=6.26, p=0.18). По лиги: 4 от
13 излизат значими поотделно (portugal p=0.0010, conference_league p=0.0107, england2 p=0.0146,
europa_league p=0.0183 - при 13 теста очаквано ~0.65 фалшиво "значими" по чиста случайност, реално 4, но
без multiple-comparisons корекция това не е автоматично доказателство) - от тях 3 (portugal,
conference_league, england2) са в посоката на хипотезата (модел надценява "под 1.5" с 3-4.5пр.п.),
europa_league е в обратната посока. Файл: `team_goals_dispersion_20260825.csv` (98 реда).

**3. `validation/most_confident_slot_distribution.py`** - преброй кой пазар печели слота "НАЙ-СИГУРЕН" на
`/daily`, върху 517 логнати мача (427 с eligible победител), реизползвайки каноничната
`pick_selection.rank_logged_rows()` (същата функция, която живият `/daily` реално вика). **Хипотезата
"под 1.5 доминира слота" НЕ се потвърждава като доминация**: `home_win` печели най-много (28.8%), после
`under25` (23.0%), после `home_under15` (21.5%), `over25` (13.1%), `away_win` (7.7%), `home_over15` (5.4%),
`draw` (0.5%). `home_over15`+`home_under15` общо: 26.9% от слотовете - забележимо, СРАВНИМО по големина с
находката "под 9.5 корнера" от 11.08.2026 (39.4%, виж H.1), но не доминиращо мнозинство както там - трите
най-чести пазара (`home_win`/`under25`/`home_under15`) са относително близки едно до друго. Забележка:
слотът гледа само `home_over15`/`home_under15` (гост-отборните версии не са в
`_TOP_PICK_SAFE_CODES`/`_raw_candidates()` изобщо - не участват в избора на "най-сигурен"). Файл:
`most_confident_slot_distribution_20260825.csv` (50 реда).

**Обща преценка (мнение, не доказан извод):** механизмът, който Дака подозира (сортиране по сурова
вероятност предпочита пазари със структурно изкривена базова честота), е реален и вероятно допринася -
но и трите измервания показват по-мека картина от първоначалната хипотеза: (2) показва малък, предимно
незначим ефект на опашката, не системно завишаване; (3) показва `home_under15` като силен, но не
единствен доминант на слота - `home_win`/`under25` печелят също толкова или повече. Няма нищо тук, което
налага промяна в прага/сортирането точно сега - но ако с времето (2) се повтори с по-голяма извадка и
остане в същата посока по повечето лиги, това би било основание да се преразгледа как се смятат
"под 1.5" вероятностите (по-широка регуляризация на опашката), не самото сортиране по слота.

---

## 13. Задача 1 (25.08.2026): нова дефиниция на "единствената прогноза" - НЕ приложена

Дака предложи: вместо "най-висока вероятност" (сегашно), картата на `/daily` да показва "най-висока
вероятност сред залозите с EV>=0", а при никой EV>=0 - изрично "нищо не струва" вместо да вади
най-вероятния на всяка цена. Изрично поиска да НЕ се прилага преди измерване. Измерено в
`validation/top_pick_rule_comparison.py`, върху 105 уредени мача (safe кодове + най-вероятният htft ред
за фикстурата, точно кандидат-пула на живата карта), CSV `top_pick_rule_comparison_20260825.csv`:

| Правило | n | win% | Brier | ROI% |
|---|---|---|---|---|
| (а) сегашно (max вероятност) | 105 | 56.2 | 0.2317 | -7.6 |
| (б) най-висок EV | 95 | 42.1 | **0.2077** | +6.4 |
| (в) предложено буквално (EV>=0, без таван) | 72 | 37.5 | 0.2230 | **-13.2** |
| (в') предложено + капирано EV<=40% (MAX_TRUSTWORTHY_EV) | 56 | 44.6 | 0.2323 | +6.7 |

**Критична находка при първото пускане:** буквалното "EV>=0" (в) без таван вадеше залози с EV до
**152%** - точно артефактите, които `MAX_TRUSTWORTHY_EV=0.40` вече е разпознал и маркира като "не
вярваме на модела си" (`validation/ev_threshold_backtest_20260824.csv`). Без този таван предложението
наследява точно проблема, който другаде в системата вече е решен - (в) на практика избира най-вероятния
сред НЕДОВЕРЕНИ извънредни EV стойности, не сред реални възможности.

**Извод: (в) буквално НЕ е по-добро от (а)** - по-нисък win rate (37.5% срещу 56.2%) И по-лошо ROI
(-13.2% срещу -7.6%), Brier сравнимо/леко по-зле. Paired bootstrap CI на разликата (в)-(а) върху същия
подпул (n=72/65) съдържа нулата и в двете посоки - недостатъчна извадка да се твърди нещо категорично, но
точковата оценка е категорично не в полза на (в). **Капираната версия (в') изглежда по-обещаващо**
(ROI точкова оценка +6.7% срещу -12.3% на (а) върху същия подпул, n=56) - но paired CI пак съдържа нулата
(n=50) - твърде малка извадка (само 56-105 уредени мача общо) да се твърди значимост.

**Решение: НЕ прилагано.** Нито (в) буквално (по-зле от сегашното с тази извадка), нито (в') (обещаващо,
но недоказано - `MAX_TRUSTWORTHY_EV` таванът е ЗАДЪЛЖИТЕЛЕН, ако някога се приложи такова правило -
буквалният вариант е показал защо). Ако с времето (в') се повтори на по-голяма извадка (жизнено се
натрупват само единици уредени мача на ден) и остане положително - тогава е моментът да се приложи, с
изричния "нищо не струва" текст на картата за случаите без EV>=0 кандидат.

---

## 14. ПРЕУСТРОЙСТВО (25.08.2026) — Стъпка 1: единствен източник за "прогнозата на мача"

Изпълнено самостоятелно (Дака не на разположение), по плана в `PREUSTROYSTVO.md`. Пълният контекст защо
преустройството изобщо е нужно е в `validation/audit_misleading_selection_20260825.md` (12 находки,
одит "механичен избор/подредба").

**Какво имаше:** три независими имплементации на "коя е прогнозата за този мач":
1. `/daily` картата - `pick_selection.rank_candidates()` (вече чрез pick_selection.py, Фаза I.1).
2. Началната страница "Топ прогнози" - `pick_selection.rank_logged_rows()` (вече чрез pick_selection.py).
3. `/results` (`group_by_match()`, `web/results.py`) и `/system_check` (инлайн в `system_check()`,
   `web/admin.py`) - **трета, отделна** логика: ръчен `max(safe_preds or publishable_preds or
   m["predictions"], key=pick_pct)`. Третото fallback стъпало заобикаляше `policy.is_publishable()`
   изцяло - показваше буквално всеки логнат пазар, включително REJECTED/corners/cards/offsides, ако
   нищо publishable съществуваше за мача. Нарушаваше документирания инвариант на самия
   `is_publishable()` ("REJECTED и NO_DATA пазари НЕ се показват никъде").

**Какво направих:** нова `pick_selection.top_pick_for_match(rows, league, policy)` - обвивка над вече
съществуващия `rank_logged_rows(n=1)`, връща `None`, ако нищо публикуемо съществува (вместо да пада до
недоверен пазар). `web/results.py` и `web/admin.py` вече викат нея, третата имплементация е изтрита.
Шаблоните (`results.html`, `system_check.html`) вече показват изрично "Няма доверена прогноза за този
мач.", ако функцията върне `None`, вместо да гърмят на `None.market_code`.

**Кои страници промениха видимото поведение и защо новото е вярното:**
- `/results` ("Резултати и ефективност") и `/system_check` - и двете. `/daily` и началната страница
  ("Топ прогнози") **не са пипани** - те вече минаваха през `pick_selection.py`, нищо не се промени там.
- Локален тест върху реалните 517 логнати мача (сравнение старо срещу ново, преди рестарт):
  - **189/517 мача** сменят кой пазар се показва като "прогнозата за мача".
  - **90/517 мача** вече показват "Няма доверена прогноза" вместо конкретен пазар: 40 `england2`, 32
    `portugal2`, 18 `germany2` - точно трите лиги, за които политиката документирано няма доверие
    (`england2`/`germany2` = UNVERIFIED, никаква ръчна класификация; `portugal2` = REJECTED навсякъде в
    `TRUST_MATRIX`, държана жива по изрично желание на Дака, но никой пазар не бие baseline).
  - Пазарите, които СТАРАТА логика показваше за тези 90 мача: double_chance (35 пъти), corners (30),
    team_total (9), cards (9), offsides (7) - структурно недоверени пазари ("двойните шансове изключени
    по бизнес причина, корнери/картони/засади системно подвеждащи" - виж `prediction_policy.py`).
  - **Защо новото е вярното:** тези 90 мача вече не разполагат с нищо, за което системата има
    основание да твърди доверие - показването на "77% сигурност" за офсайд пазар в `england2` (лига без
    статистическо покритие) е точно артефактът, който целият този одит/преустройство цели да спре.
    "Няма доверена прогноза" е честният отговор, не мълчаливо падане до първия попаднал ред.

**Тест/рестарт/проверка:** синтактична проверка (`ast.parse`) на трите питон файла, локално сравнение
старо/ново върху реалните данни (виж числата по-горе), `kill -HUP` рестарт в 11:01:38 UTC, `journalctl`
чист (само нормалното зареждане на 17 модела от кеша, "Всички модели са в кеша."), функционален тест
през автентикирана сесия: `/results`, `/results?sort=edge`, `/system_check`, филтрирани по
`f_league=england2` - всички 200, "Няма доверена прогноза за този мач." се показва коректно (33 пъти на
`/system_check?f_league=england2`, 30 пъти на `/results?f_league=england2`), без traceback/грешка в
изхода. `/`, `/daily`, `/value`, `/my_bets` също проверени (непипнати, но за здравна проверка) - всички
200. Commit `0d75326`.

## 15. ПРЕУСТРОЙСТВО (25.08.2026) — Стъпка 2: махане на неверните твърдения

Продължение на Раздел 14, все така самостоятелно изпълнение. Commit `cb6525d`.

**`/system_check` отпада от главния поток (не се трие маршрутът):**
- Изтрити от `web/admin.py`/`templates/system_check.html`: "Успеваемост" (общ win_rate върху суровия
  `predictions_log`), "Най-добри пазари"/"Най-слаби пазари" (`by_market`/`by_league` от
  `st.get_stats_by_market()`/`get_stats_by_league()`) - Находка 1 от одита: структурен артефакт, различните
  пазари имат различна базова вероятност за позна(ва)не, смесването им в едно число подвежда.
  `system_tracker.get_stats_by_market()`/`get_stats_by_league()` остават непипнати в кода (само вече
  никой не ги вика от `/system_check`) - CLAUDE.md забранява промяна в `system_tracker.py` "на око".
- Всички водещи връзки към `/system_check` премахнати: картата на `index.html` (заменена с връзка към
  `/results`), навигацията на `templates/refresh_status.html` (заменена с "📋 Резултати"). `templates/
  match_result.html`'s "← Обратно към проверка" **остана непипнат** - `/match_result` се достига само от
  `/system_check`'s карти, контекстуално вярно да сочи обратно натам.
- Маршрутът `/system_check` **остава жив** (само вече е недостъпен от менюто/началната страница) -
  таба "Мачове" (търсене/филтри/пагинация) е реално полезен и вече минава през единствения избор от
  Стъпка 1 (`pick_selection.top_pick_for_match()`), не се трие.

**`/results` текстът "не корнери/картони/засади" - потвърдено вярно (Находка 3):** локална проверка на
живите 517 логнати мача - 0 корнери/картони/засади излизат като `top_pred` след Стъпка 1
(`TRUST_MATRIX` ги маркира REJECTED/NO_DATA във всяка лига, `trust_derived` няма нито един измерен ред
за тези три групи). Самият текст беше подвеждащ в друга посока - твърдеше "само 1X2/над-под 2.5", а
реално `team_total` (отборни голове над/под 1.5) излиза топ прогноза **по-често от всичко друго**:
188/517 мача срещу 132 за 1X2 и 104 за над/под 2.5 (плюс 3 btts). Текстът пренаписан да отразява
реалността, без да претендира изчерпателен списък от пазари.

**Мъртъв код изтрит (Находка 8):** `calibration_table()` в `web/results.py` (заменена от
`evaluation.summary()` на 11.08.2026, никога не се рендираше в шаблона - потвърдено с `grep` преди
трита). `overall_stats()` вече връща само `settled`/`pending` - единствените реално четени полета;
`win_rate`/`roi`/`profit`/`avg_odds` там смятаха върху суровия `predictions_log`, същия артефакт като
Находка 1, никога показвани.

**`KELLY_FRACTION`/"препоръчан залог (Kelly)" маха от интерфейса:** единственото място, което го
показваше, беше колоната "Залог (Kelly 25%)" в `templates/match_detail.html` ("💎 Разлики с пазара"
таблицата) - изтрита заедно с обяснителния текст под таблицата. Константата остава в
`match_predictor_app.py` (`KELLY_FRACTION = 0.25`), самото изчисление (`kelly_stake`) остава в кода -
просто вече не се рендира никъде. Причина: размерът на залога предполага, че нашата вероятност е
вярна - точно твърдението, което трите независими измервания от 24-25.08 (Раздели 11-12) показаха като
недоказано.

**Тест/рестарт/проверка:** `ast.parse()` на двата питон файла, `jinja2.Environment.get_template()` за
всички 5 пипнати темплейта (компилира без грешка), `kill -HUP` рестарт в 11:08:24 UTC, `journalctl`
чист. Функционален тест през автентикирана сесия: `/system_check` (200, 0 "Успеваемост"/"Най-добри/
Най-слаби" в изхода извън обяснителния текст), `/results` (200), `/` (200, 0 връзки към `/system_check`
в изхода), `/refresh_status` (200), `/match_detail?league=spain&fixture_id=1570342...` (200, 0 "Kelly"
в изхода, без traceback). Commit `cb6525d`.

**Следва:** Стъпка 3 от `PREUSTROYSTVO.md` (свиване на менюто до четири страници).

---

## 16. ПРЕУСТРОЙСТВО (25.08.2026) — Стъпка 3: свиване на менюто до четири страници

Продължение на Раздели 14-15, самостоятелно изпълнение. Commit `ae1a8e3`.

**Нов sidebar (идентичен блок във всичките 12 `templates/*.html`, плюс новия `system_hub.html` = 13):**
само три директни връзки - 📅 Днес (`/daily`), 📋 Как се справяме (`/results` - преименувана от
"Резултати и ефективност", title+h1 също обновени), 🎯 Моите залози (`/my_bets` - **НОВО в менюто**,
преди изобщо не беше линкнат от sidebar въпреки че страницата отдавна съществуваше). "Мачът"
(`/match_detail`) няма собствена sidebar връзка - изисква контекст на конкретен мач (fixture_id/лига/
отбори), достъпва се през ред от списък, точно както преди - "без промяна", както плана изрично казва.

**Нова скрита страница "⚙️ Система" (`/system`, `web/admin.py:system_hub()` +
`templates/system_hub.html`):** извън основния nav поток, само страница с линкове - Търсене
(`/manual`), Лиги и опресняване (`/leagues_admin`), Диагностика (`/diagnostics`). Съзнателно НЕ прави
собствено живо API `/status` повикване (щеше да добави нов консуматор на дневната квота, виж Раздел 8) -
"ръчно опресняване" и "състояние на квотата и задачите" от плана вече съществуват готови на
`/leagues_admin` (refresh бутон + running banner) и `/diagnostics` (API квота, DB/модели/услуги статус) -
`/system` само сочи към тях, не дублира логика.

**Маршрутите НЕ са изтрити** - `/`, `/value`, `/manual`, `/leagues_admin`, `/diagnostics`,
`/system_check`, `/live`, `/match_result` продължават да работят по директен URL, само излизат от
sidebar менюто. `templates/refresh_status.html`'s собствена (по-стара, отделна от sidebar системата)
навигационна лента също опростена до същите 4 елемента, за консистентност.

**Съзнателно НЕ пипнато в тази стъпка** (извън обявения обхват "свиване на МЕНЮТО" - изисква
съдържателно продуктово решение, не просто пренареждане на nav):
- "Разлики с пазара влива се в 'Днес'" - това е буквално Стъпка 4 от плана ("Списъкът с разлики...
  става филтър тук"), не Стъпка 3. `/value` продължава да съществува отделно засега.
- "На живо остава таб в 'Днес'" - планът не детайлизира КАК точно (нов таб в `/daily`'s UI? какви
  данни?) - не е измислено на място, оставено отворено за Стъпка 4 или изрично решение на Дака.
- "Резултат от мач влива се в 'Мачът'" - също не детайлизирано никъде в `PREUSTROYSTVO.md`.
  `/match_result` остава отделен маршрут, достъпен само чрез `/system_check`'s карти на завършени
  мачове (виж Раздел 15 - `system_check` вече не е в nav, но остава функционален).

**Тест/рестарт/проверка:** `jinja2.Environment.get_template()` за всички 16 `.html` файла в
`templates/` (компилират без грешка), `ast.parse()` на `web/admin.py`, `kill -HUP` рестарт в
11:14:12 UTC, `journalctl` чист. Функционален тест през автентикирана сесия: `/system`, `/daily`,
`/results`, `/my_bets`, `/value`, `/manual`, `/leagues_admin`, `/diagnostics`, `/live`, `/system_check`,
`/refresh_status`, `/` - всички 200 (`/match_result?fixture_id=1` коректно 302 towards `/system_check`,
несъществуващ fixture, очаквано поведение). Проверено активното sidebar подсветяне поотделно за всяка
от четирите нови nav точки (`class="active"` присъства точно на очаквания елемент за всеки маршрут).
Commit `ae1a8e3`.

**Следва:** Стъпка 4 от `PREUSTROYSTVO.md` ("Днес" в окончателен вид - обединяване на "Разлики с
пазара" като филтър в `/daily`, единна дефиниция за "стойност").

---

## 17. ПРЕУСТРОЙСТВО (25.08.2026) — Стъпка 4: "Днес" в окончателен вид

Продължение на Раздели 14-16, самостоятелно изпълнение. Commit `fe8052a`.

**Скоуп решение, взето преди да пипна код:** планът за тази стъпка ("Списъкът с разлики... става
филтър тук", "На живо остава таб в 'Днес'", "Резултат от мач влива се в 'Мачът'" - последните две от
Стъпка 3) не детайлизира ВСИЧКО (напр. точно как изглежда "таб На живо" вътре в `/daily`). Затова
ограничих тази партида строго до частите, изрично описани с достатъчен детайл в `PREUSTROYSTVO.md`
Стъпка 4 - реда на мача, филтъра, единната дефиниция за "разлика". "На живо като таб" и "Резултат от
мач влива се в Мачът" **остават НЕ направени** - не бяха достатъчно детайлизирани, за да ги направя без
да гадая продуктово решение, докато Дака е недостъпен (виж СПРИ условието в задачата).

**`build_pick_card()` → `build_diff_row()` (`match_predictor_app.py`):** старата функция избираше ДВА
реда - "сигурен" (най-висока вероятност) и "стойностен" (най-висок ПОЛОЖИТЕЛЕН EV, с прагове
`MIN_VALUE_BET_PROB`/`MAX_VALUE_BET_ODDS` без цитирано доказателство - Находка 5). Новата избира ЕДИН
ред - пазара с НАЙ-ГОЛЯМА АБСОЛЮТНА разлика спрямо кешираната пазарна цена (само сред
home_win/draw/away_win/over25/under25, единствените с `market_odds`), без значение накъде е разликата.
Ако никой доверен пазар няма пазарна цена - връща топ доверения кандидат по вероятност, с
`market_pct`/`diff` = `None` (мачът все пак показва нещо - "пазар" стъпалото на pick_selection вече е
приложено по-рано, картата просто няма с какво да сравни). `MIN_VALUE_BET_PROB`/`MAX_VALUE_BET_ODDS`
константите остават в кода (използвани все още от `compute_grouped_markets()`'s value_bets таблица за
`/match_detail`, НЕПИПНАТА в тази стъпка - виж скоуп решението по-горе).

**`templates/daily.html`:** редът на всеки мач вече е "{пазар} {наш %} · пазар {пазарен %} · разлика
{+/-X.X%}" - без 💎/🎯 етикети, без "ако нашата оценка е вярна: за 100 лв → X лв" рамка (точно
препоръчителната рамка, която целият одит цели да спре).

**Филтър "Само където се разминаваме с пазара" (`diff_only`):** нов checkbox на `/daily`, реюзва
БУКВАЛНО СЪЩАТА `get_value_opportunities()` функция като `/value` и началната страница (PROVEN-само,
вероятност 25-85%, edge 3-15% - `web/value.py`) - едно място за "разминаваме се с пазара" вместо
отделна логика (Находка 4). Прилага се само на `upcoming_matches` (не live/finished/skipped) -
`get_value_opportunities()` изключва вече започнали мачове по дизайн (`match_date >= сега`), затова
приложен широко би скрил грешно текущите live мачове.

**Меню за подредба:** преименувано, никоя опция вече не звучи като "по-добри залози" - "По час (по
лиги)" (без промяна на самата логика), "По разлика с пазара (най-голямо разминаване)" (било "най-висок
EV"), "По вероятност (най-висок %)" (било "По сигурност"). `web/daily.py`'s `_daily_sort_key()` за
`sort=value` вече чете `card["diff"]` (абсолютна стойност), не EV - едно определение за "разлика",
използвано и в реда, и в подредбата.

**Тест/рестарт/проверка:** локален тест на `build_diff_row()` срещу синтетични данни (3 случая: избор
на най-голямата абсолютна разлика измежду 5 пазара с реални цени - потвърдено ръчно преизчислена
devig аритметика; fallback без пазарни цени връща топ вероятност с `market_pct=None`; празен списък
връща `None`) - всички минаха. Отделен тест на `_daily_sort_key('value')` върху синтетични мачове
(проверено `abs()`, `-inf` при липса). `kill -HUP` рестарт в 11:22:32 UTC, чист `journalctl`.
Функционален тест през автентикирана сесия: `/daily` във всички комбинации sort×diff_only (200),
проверено в самия HTML изход - старите бадж текстове ("Най-сигурен", "за 100 лв") отсъстват, новите
sort етикели присъстват, `diff_only=1` реално намалява броя upcoming мачове (4→2 в живата тестова
извадка), `sort=value` реално подрежда по низходяща `|разлика|` (проверени числа: 5.1/3.8/2.4/1.5/1.3/
0.9%). `/`, `/value`, `/match_detail` проверени непокътнати (200). Commit `fe8052a`.

**Следва:** Стъпка 5 от `PREUSTROYSTVO.md` ("Как се справяме" казва истината - добавяне на Brier срещу
пазара по лига/пазар с брой наблюдения, разделение доказано/кандидат/недостатъчно данни, изрично
изречение за липса на доказано предимство).

---

## 18. ПРЕУСТРОЙСТВО (25.08.2026) — Стъпка 5 (последна): "Как се справяме" казва истината

Продължение на Раздели 14-17, самостоятелно изпълнение. Commit `0e8be3d`. **Всичките 5 стъпки от
`PREUSTROYSTVO.md` са затворени с тази партида** - виж Раздел 19 по-долу за пълния доклад.

**Констатация преди да пипна код:** страницата вече съдържаше почти всичко изисквано, от по-ранна
работа на 24.08.2026 (Раздел 11, "Точка 1" от разговора с Дака) - секцията "Бием ли пазара?"
(`brier_vs_market.py`, `web/results.py:brier_vs_market_table()`, tab=`effectiveness`) вече показваше
Brier срещу пазара по лига И пазар, с брой наблюдения, разделение доказано/кандидат/недостатъчно данни
(праг `MIN_N=30`), split-half методология за "доказано". `evaluation.summary()` вече даваше честните
"Познати"/ROI/Brier KPI-та. `roi_by_market()`/`roi_by_league()` вече минаваха през
`evaluation.published_picks()` (Партида 2, О.1, 21.08.2026) - "успеваемост по сурови редове" вече не
съществуваше на тази страница (потвърдено с `grep` - думата "Успеваемост" отсъства навсякъде в
`results.html`, win_rate колоните са надписани "Познати" и идват от публикуваните прогнози, не от
суровия `predictions_log`).

**Единствената реална липса:** изрично изречение НАЙ-ОТГОРЕ на страницата, не заровено в
tab=`effectiveness` (където живее "Бием ли пазара?"). Добавен банер (`.rv-warn`, жълт, като другите
предупреждения на страницата) точно под `<h1>Как се справяме</h1>`, видим на ДВАТА таба (проверено
живо): "**Системата няма доказано предимство пред пазара.**" + едно изречение резюме на трите
независими измервания от 24-25.08.2026 (смесване с пазара подобрява навсякъде; единствената лига с
достатъчно данни е по-зле от пазара; никой EV диапазон не показва значима печалба) + препратка към
детайлната секция по-долу.

**Тест/рестарт/проверка:** `jinja2.Environment.get_template()` за `results.html`, `kill -HUP` рестарт в
11:26:16 UTC, чист `journalctl`. Функционален тест през автентикирана сесия: `/results` и
`/results?tab=effectiveness` (200), банерният текст присъства и в двата отговора (`grep -c` = 1 и на
двата). Commit `0e8be3d`.

---

## 19. Тайните извадени от кода, завъртени, и почистени от archive/ (25.08.2026)

Продължение на Раздели 14-18, същата дата, изрично поискано от Дака (не спада към петте стъпки на
преустройството, отделна задача). Commit-ове `ba2c7fe`..`be51b7d`, после `0861b3f`.

- Извадени в `.env`/`config.py`: API-Football ключ, `LOGIN_PASSWORD`, `REFRESH_TOKEN`, Flask
  `secret_key` (последните два открити по пътя, не бяха изрично поискани, но живееха в същия ред код -
  включени по същата логика). Виж Раздел 5 за пълния механизъм и как Дака сменя стойностите занапред.
- Дака завъртя и четирите стойности на живо СЛЕД извеждането - потвърдено с реални тестове (жива
  API-Football заявка, `refresh_odds_cron.sh`, `/refresh_all`, логин с новата парола) - старите вече
  не работят никъде (302/200-отказ навсякъде проверено).
- Хранилището вече е публично (`git@github.com:jnpetkov-tech/zalog.git`) - старият (вече мъртъв) API
  ключ и старата парола почистени и от 34-те мъртви скрипта в `archive/` (никога не бяха живо
  изпълнявани, но текстово стояха там) + от `PROJECT_STATUS_FOR_REVIEW.md`. Финална проверка (`grep -rn`
  за всичките четири стари стойности, целия репо без `venv/`/`.git`) - нула съвпадения.
- **Ново правило за протокола:** `git push` след всеки commit (не само commit) - хранилището има
  `origin` remote отсега нататък, вижте `CLAUDE.md` правило 4.

## 20. Довършване на преустройството - четирите оставащи точки от Раздел 16 (25.08.2026)

Същата дата, изрично поискано от Дака - затваря трите точки, оставени съзнателно отворени в Раздел 16
("съзнателно НЕ пипнато") плюс една нова (item 4). Commit-ове `a067e17`, `eed50ab`, `96c8cfd`.

1. **`/value` отпада от навигацията** (commit `a067e17`) - маршрутът остава жив (директен URL), но
   вече никъде не води до него (последната връзка - `index.html`'s дисклеймър текст - премахната).
   Причина: филтърът "Само където се разминаваме с пазара" в `/daily` (Стъпка 4) вече върши същата
   работа - "две места за едно нещо е точно болестта, която лекуваме" (Дака).
2. **`/live` - НЕПИПНАТ**, изрично по решение на Дака ("живите мачове са отделно нещо и сливането им
   иска собствено обмисляне").
3. **`/match_result` се влива в `/match_detail`** (commit `eed50ab`) - "един мач, една страница".
   `/match_detail` вече показва краен резултат + пълния списък логнати прогнози (СЪЩАТА заявка,
   `st.get_predictions_for_fixture`), сгънато по подразбиране, само когато мачът вече е приключил.
   `/match_result` вече само пренасочва (302) към `/match_detail` със същите параметри - старите връзки
   (`system_check.html` картите) работят непроменени. `templates/match_result.html` остава в репото,
   вече не се рендира отникъде.
4. **`/match_detail`'s "Разлики с пазара" вече минава през същата дефиниция като "Днес"** (commit
   `96c8cfd`) - `compute_grouped_markets()`'s `value_bets` вече сортира по НАЙ-ГОЛЯМА АБСОЛЮТНА разлика
   (build_diff_row()'s дефиниция), не по EV с `MIN_VALUE_BET_PROB`/`MAX_VALUE_BET_ODDS` (Находка 5,
   последната останала употреба на тези два прага - вече изцяло неизползвани, оставени декларирани в
   кода). `MAX_TRUSTWORTHY_EV`/"Пренебрегнати оценки" непипнати - отделна, доказана проверка за модел-
   артефакт, не дефиниция за "разлика".

Плюс почистване, комбинирано в commit `a067e17`: изтрита мъртвата `SIDEBAR_HTML`/`SIDEBAR_STYLE`
константа от `match_predictor_app.py` (неизползвана още от Партида 5, 21.08.2026).

**Резултат: и петте стъпки от `PREUSTROYSTVO.md` (Раздели 14-18) плюс всичките точки от Раздел 16 "какво
е останало недовършено" (с изключение на `/live`, изрично оставен) вече са затворени.**

---

## Здравна проверка към 22.08.2026, ~04:50 UTC (край на тази сесия)

- `match-predictor-app.service`: **active**, gunicorn master+worker живи.
- Всички проверени маршрути връщат 200 през автентикирана сесия, 302 (login redirect) без сесия — точно очакваното: `/`, `/manual`, `/daily`, `/live`, `/my_bets`, `/system_check`, `/leagues_admin`, `/diagnostics`, `/results`, `/value`, `/refresh_status`.
- `journalctl` за целия ден чист от грешки, освен вече документирания и оттогава решен `incremental_refresh` инцидент (виж раздел 3).
- `predictions.db`: цялост `ok`. Дисково пространство: 82.5 GB свободни от 96.9 GB (85%). Всички системни услуги (`match-predictor-app`, `cloudflared`, `check-results.timer`, `predictions-server`) — active.
- Всичките 6 systemd timer-а (`refresh-odds`, `build-predictions-snapshot`, `nightly-snapshot`, `build-trust-derived`, `refresh-pending-odds`, `db-backup`, `check-results`) — заредени и разписани коректно.
- API-Football дневна квота: ~4270/7500 останали.
- `git status`: чисто work tree, всичко commit-нато.

**Системата е жива и здрава. Целият оригинален план е изпълнен.**
