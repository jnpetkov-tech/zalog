# CLAUDE_HANDOFF.md — прочети това първо

Снимка към **16.08.2026 вечер**. Ако виждаш тази бележка на по-късна дата и не са минавали редовни обновявания — с голяма вероятност съдържанието долу вече е остаряло в детайлите (нови лиги, нови фази), но протоколът и предупрежденията остават валидни.

Ти си Claude Code, стартиран директно на живия production сървър на този проект. Нямаш достъп до предишни чат разговори с Дака, нито до claude.ai Project-а, в който живее пълната история и план (`ACTION_PLAN_v2.md`, `PROJECT_STATE.md` и други). Този файл е самостоятелна снимка, писана специално така, че да можеш да продължиш работа без нищо друго.

**Дака (собственикът) е нетехнически.** Разчита изцяло на теб за технически решения. Това означава повишена отговорност: обяснявай простичко, не крий грешки, не бързай с промени по живия сървър без тестване.

---

## 1. Какво е това нещо

Flask приложение за прогнози на футболни резултати (`match_predictor_app.py`), базирано на Poisson модели (`football_lib.py`), обслужвано през gunicorn, зад Cloudflare на домейн `zalog.dd26.eu`.

- Хост: `szalozi`, потребител: `inkas`, работна директория: `~/sportbg-predictor`
- gunicorn: `--workers 1 --threads 4 --timeout 120 --bind 127.0.0.1:8001`
- systemd service: `match-predictor-app.service`
- SQLite база: `predictions.db` (WAL режим, `busy_timeout=5000`), достъп през `system_tracker.py` (внесен като `st`)
- Данни: CSV файлове по лига, `{league}_merged_full.csv`, обновявани нощно
- Модели: фитват се лениво при първо извикване, кешират се като pickle в `MODEL_CACHE_DIR`
- Външен API: API-Football (`v3.football.api-sports.io`) — платен, rate-limited

## 2. Регистър на лигите

`ALL_LEAGUES` и `LEAGUE_FLAGS` в `match_predictor_app.py` (около ред 85-113) — 17 лиги към 16.08.2026: bulgaria, england, germany, spain, france, champions_league, europa_league, conference_league, italy, portugal, и вторите дивизии france2/spain2/italy2/portugal2/bulgaria2/england2/germany2.

**Важен урок (виж И.2 по-долу):** скриптове за теглене на данни (`fetch_all_second_divisions.py`) могат да изтеглят CSV за лига, която НЕ е регистрирана в `ALL_LEAGUES` — данните лежат на диска, но никой не ги вижда, докато не се добавят изрично. Ако Дака каже "изтеглихме X, защо го няма" — първо провери и `ALL_LEAGUES`, и дали има сестрински лиги в същия скрипт, които също може да липсват (случи се точно това с Германия 2 и Англия 2 едновременно).

## 3. Статус на фазите (към 16.08.2026)

### Готово и живо
- **H** — филтриране на невалидни/отхвърлени пазари от показването
- **I.1-I.3** — `pick_selection.py` (каноничен избор на прогноза), `evaluation.py` (честни метрики), wiring на началната страница и `/results`
- **J.1** — коефициентите се теглят за прозорец от 7 дни напред, не 2
- **N.1** — предпазител: EV над 40% не влиза в препоръчаните залози, а в отделен "⚠️ Пренебрегнати оценки" списък (виж `MAX_TRUSTWORTHY_EV` в `match_predictor_app.py`). Мотивиран от реален случай: моделът показа ЦСКА 1948 срещу Панатинайкос с EV +90.1%, което беше грешка, не находка.
- **И.1** — хотфикс за rate limit/524 срив на `/daily?league=all`: премахнати живи API извиквания на мач (contузии вече през `injuries_cache`, логването на пазари вече използва вече изтегления `cached_odds` вместо нова заявка)
- **И.2** — добавени `england2` и `germany2` в регистъра (виж т.2 по-горе)
- **J.2, I.4, N.3** — виж "Обновено на 20.08.2026" по-долу за детайли

### Диагностицирано, НЕ поправено (умишлено)
(празно - N.2 е поправено на 20.08.2026, виж K.1 по-долу)


### Открито, все още непоправено
- **И.3 (остатък)** — първата (студена) заявка на `/daily?league=all` за 7 дни напред остава бавна (~100-125 сек дори с топли модели, gunicorn на ~130% CPU сустейнд) - виж "Обновено на 21.08.2026" по-долу за какво вече е оправено (кеш при повторни зареждания). Остатъчният проблем е чисто структурен: `--workers 1 --threads 4` + вътрешен `ThreadPoolExecutor(max_workers=8)` за 17-те лиги означава, че реалната Python сметка (Poisson матрици, Dixon-Coles, ht/ft, htft за 100+ мача) се сблъсква с GIL - нишките не се изпълняват наистина паралелно за CPU-bound код, само numpy C-извикванията частично освобождават GIL. Кандидати за бъдеще, ако студеният старт (веднъж на 5 мин) все още дразни Дака: (а) `ProcessPoolExecutor` вместо `ThreadPoolExecutor` за фактическа паралелност между ядра (по-рисково - моделите/кешът трябва да са pickle-able и се дублират по процес), (б) векторизация на per-match цикъла с numpy върху всички мачове на лига наведнъж вместо ред по ред.

### Обновено на 20.08.2026
- **J.2** — готово. `check_odds_coverage.py` (нов, само диагностика, не пипа живите файлове) показа 79% общо покритие с коефициенти за прозореца от 7 дни. germany/germany2 показаха 0 мача — провери се директно през API, оказа се легитимно (следващият кръг е на 1 ден извън прозореца), не бъг в регистъра. Реални дупки: europa_league/conference_league (~50%, плейоф кръг на границата на прозореца, букмейкърите още нямат линии), italy2 (~50%, вероятно API-Football просто няма пазари за Серия Б). Не изисква код промяна, само за информация.
- **I.4** — готово. `evaluation.weekly_brier()` (нова функция) + KPI елемент "Brier score" и графика "по седмици" на `/results?tab=effectiveness`. Текущо общо: 0.212 на база n=51 settled публикувани прогнози.
- **N.3** — готово. (а) нова `run_refresh_injuries_cache()` + `/refresh_injuries_cache` route, извикван от `refresh_odds_cron.sh` (същият 30-мин timer `refresh-odds.timer` - НЕ е създаден нов systemd timer, нарочно, за да се избегне нужда от sudo/root). Прозорец 48ч, всички лиги еднакво (контузиите са информативни, не вход за модела освен където `has_injuries` вече е вярно - това гейт не се промени). Първи тестов run: 100 проверени, 32 с намерени данни. (б) `/match_detail` вече чете кеш-първо (`st.get_cached_injuries`, само при изтекъл кеш живо API извикване) и показва бележка "🩹 Контузии: ..." над прогнозата, за всички лиги. Тествано на живо (Genoa-Napoli, italy) - показва коректно, без грешки в логовете.
- **I.3 (остатък)** — готово. `evaluation.summary()` вече връща и `profit`/`avg_odds`, смятани само от публикуваните settled прогнози (същия набор като `roi`/`brier`). На `/results` "Чиста печалба" и "Среден коеф." (и на двата таба) вече ползват тези честни стойности вместо суровите редове от `predictions_log` - предупредителната бележка за разминаване е премахната (вече пасват точно, проверено на живо: +1.8 ед. / 15 публикувани = +12.1%, съвпада с "Възвръщаемост"). Съзнателно НЕ пипнато в тази стъпка: `roi_by_market()`/`roi_by_league()`/`weekly_roi()` ("По пазар"/"По лига"/"Възвръщаемост по седмици") все още смятат по суровите редове (всеки логнат пазар, не само публикувания) - същият структурен проблем, но извън обхвата на изрично отбелязания TODO. Кандидат за отделна бъдеща стъпка, ако Дака иска пълно изравняване.
- **K.1 (+ N.2 поправка)** — готово, най-мащабната промяна досега (пипа `fit_goals_model()` в `football_lib.py`, ползван от ВСИЧКИ 17 лиги). Две промени, комбинирани умишлено (и двете пипат същата функция, минаха общ backtest):
  1. **Dixon-Coles rho**: фитва се допълнителен параметър заедно с attack/defence/home_adv, коригира известната системна грешка на независимия Poisson модел при ниски резултати (0-0/1-0/0-1/1-1). Прилага се навсякъде, където живото приложение реално строи вероятностите (`_raw_candidates`, `compute_grouped_markets`), не само вътре във football_lib.py.
  2. **Регуляризация зависима от обем данни на отбор** (N.2 поправка): отбор с малко "пресни" наблюдения вече се дърпа силно към "среден отбор" вместо да дава екстремна lambda. Потвърдено на живи данни: за отбор с ~0 тегло данни в шампионска лига, максималната увереност на модела падна от 61.4% на 55.6%.

  **Валидация преди деплой:** твърдението "никъде регресия" по-долу е ПОПРАВЕНО на 21.08.2026 - оригиналният backtest от 20.08.2026 никога не беше запазен във файл или commit (само терминален изход в сесия, която вече не съществува), затова числата "bulgaria OU +3pp, england 1X2 +0.8pp, italy2 +0.3/+1.3pp" не можаха да бъдат проверени и вероятно бяха % точност (accuracy), не Brier/log-loss.

  **Реален walk-forward backtest, пуснат на 21.08.2026** (скрипт `k1_walkforward_backtest.py`, резултат `k1_walkforward_backtest_results.csv`, и двата запазени в git): последните 60-200 мача на всяка от 13-те засегнати лиги (england/germany/spain/france ползват `fit_goals_direct_covariate()`, структурно недосегаема от K.1 - изключени от теста), walk-forward с преизчисляване на всеки 20 мача. Резултат - **разликите старо/ново са малки в двете посоки, не еднопосочно подобрение**:
  - 5 от 8 PROVEN 1X2 пазара (bulgaria, italy, portugal, france2, bulgaria2) показват МИНИМАЛНО влошаване на Brier/log-loss (0.0008-0.0035 Brier) - в рамките на шум за извадки от 124-200 мача, но не "регресия никъде".
  - champions_league и italy2 (PROVEN 1X2) показват подобрение; OU2.5 подобрява навсякъде осезаемо за champions/europa/conference league (+0.009 до +0.013 Brier).
  - Единствената по-голяма регресия: conference_league 1X2 (-0.0094 Brier, -0.013 log-loss) - но този пазар вече е REJECTED в TRUST_MATRIX (не бие baseline, не се публикува), затова без практически ефект.
  - `use_dc=False, low_data_extra_reg=0.0` възпроизвежда старото поведение точно (< 1e-5 разлика в lambda/mu) - обратна съвместимост потвърдена преди промяна на дефолтите (това остава вярно).

  **Извод:** промяната не е катастрофална и общата преценка (да остане деплойната) си остава разумна - магнитудите са дребни и OU2.5 е чисто по-добър почти навсякъде - но твърдението "никъде регресия" беше преекспониран извод от изгубени данни, не факт. TRUST_MATRIX класификациите (PROVEN/WEAK/REJECTED) остават валидни - нито един пазар не сменя категория заради тези разлики.

  **Съзнателно извън обхват**: `fit_goals_direct_covariate()` (моделът с контузии като covariate, ползван за лиги с `has_injuries=True`, напр. england) НЕ е пипнат - получава rho=0.0 навсякъде (старо поведение точно). Ако искаш DC/регуляризация и там, е отделна бъдеща стъпка. TRUST_MATRIX в `prediction_policy.py` **не е обновена** - backtest резултатите не показаха нито един пазар да сменя страна (бие/не бие baseline), затова текущите PROVEN/WEAK/REJECTED класификации остават валидни.

  **⭐ Капан, на който се натъкнах и е важно да се помни:** `model_cache/*.pkl` файловете се зареждат от диска БЕЗ преизчисляване, ако pickle mtime > CSV mtime (обичайният случай) - промяна само в `football_lib.py` НЕ форсира преизчисляване автоматично! Наложи се ръчно `rm model_cache/*.pkl` преди рестарт, за да влезе новият код в сила. **Ако пипаш `fit_goals_model()`/`fit_total_model()`/`fit_goals_direct_covariate()` в бъдеще, винаги изчиствай `model_cache/*.pkl` преди рестарт, инак ще тестваш стария код без да разбереш.**

  **Странична бележка за производителност:** преизчисляването на всичките 17 лиги сега отнема ~2м 47с (вместо мигновено от кеш) заради допълнителния rho параметър - еднократен разход при студен кеш/нощно обновяване на данните, не е проблем сам по себе си. Тествах `/daily?league=all` след деплоя - не завършва в рамките на 2 минути дори с топъл кеш, но единични лиги (bulgaria 4.2с, england 3.7с) отговарят нормално; по-голямата `conference_league` (49 мача) отне 16-36с самостоятелно. Това съвпада по характер с вече документирания **И.3** проблем (виж по-долу) - не изглежда да е нова катастрофална регресия от K.1, но е възможно да е леко влошило вече съществуващ проблем. Препоръка: обмисли да качиш И.3 по-нагоре в опашката, ако Дака вижда `/daily?league=all` бавен на живо.

- **L.3** — готово. Нова `prediction_policy.is_proven()` (само PROVEN, без WEAK fallback) - подадена на `get_value_opportunities()` в `value_view.py` вместо старото `is_publishable()` (PROVEN+WEAK). `/value` вече показва само доказани при backtest пазари; проверено на живо - 78 → 39 възможности, всичките премахнати бяха WEAK. Банерът на страницата обяснява защо. Причина: `/value` твърди конкретен количествен edge% - "недостатъчно тествано" (WEAK) + количествена печалба е двойна несигурност.
- **N.4 (етапи 1-2)** — готово. Продуктови решения, взети изрично от Дака: (1) бележката се добавя направо на страницата на мача (`/match_detail`), не на отделна watchlist страница; (2) "пропусни мача" мести мача в отделен раздел "🚫 Пропуснати", не го крие напълно и не оставя банер на място. Реализация: нова таблица `match_notes` (`fixture_id`, `note`, `skip`, `updated_at`) в `system_tracker.py` (`set_match_note`/`get_match_note`/`get_all_match_notes`). Нов route `/save_match_note`. `/match_detail` показва форма (textarea + чекбокс) в кутия "📝 Твоя бележка за мача". `/daily` филтрира мачове със `skip=True` от live/upcoming/finished ПРЕДИ групиране по лига и ги показва в 4-ти таб "🚫 Пропуснати" с бележката и линк обратно към мача (за преглед/връщане). Категоричната забрана е спазена буквално - никъде не се пипа `pick_pct`/prediction, само визуално местене. Тествано на живо (Genoa-Napoli, italy) - запис, показване, преместване в таба, всичко коректно, логовете чисти. **Бел.:** тестовите данни за Genoa-Napoli са изчистени (note=None, skip=False) след теста.

### Обновено на 21.08.2026
- **И.3** — две стъпки, комбинирани умишлено (git commit-и `de42ed4`, `4ee07c5`):
  1. `_predict_matches_for_league()` вече проверява `st.already_logged(fixture_id)` ПРЕДИ да вика `compute_grouped_markets()` (не след) - преди се смяташе за всеки мач при всяко зареждане, само резултатът се изхвърляше ако мачът вече е бил логнат. Логването остава побитово идентично (доказано с локален тест преди деплой).
  2. Нов TTL кеш (300 сек) на сглобения резултат от `_predict_matches_for_league()`, по ключ (лига, from_date, to_date). При cache hit връща SHALLOW COPY на всеки match dict (не same obj) - `daily()` мутира резултата in-place (`date_label`/`note`/`skipped`/`idx`), без копие мутациите от една заявка биха "изтекли" в кеша.

  **Защо TTL=300, не 60-120 както първоначално планирано:** измерено на живо, пълно `/daily?league=all` отнема ~100-125 сек дори с топли модели - при по-кратък TTL най-рано обработените (от 17-те паралелни) лиги вече изтичат преди цялата заявка да приключи, и веднага следваща заявка пак пресмята почти всичко.

  **Резултат, измерен на живо:** `/daily?league=all` студено ~115 сек (непроменено - това е еднократният разход при изтекъл/празен кеш), веднага следваща заявка в рамките на TTL прозореца - **0.19 сек** (беше същите ~115 сек преди тази стъпка). Единична лига (bulgaria) - под 0.15 сек и студено, и топло (вече кеширана от "all" преход). journalctl чист през целия тест, съдържанието на страницата проверено визуално (брой мачове, липса на грешки).

  **Компромис, приет съзнателно:** живи мачове (elapsed/резултат) може да изостанат до 5 мин при чести зареждания в рамките на TTL прозореца - приемливо за личен инструмент, не live-betting табло с посекундна точност.

  **Остава открито** (виж "Открито, все още непоправено" по-горе): самата студена сметка (~115 сек за "всички лиги") не е ускорена - само повторните зареждания в рамките на 5 мин. Структурната причина (GIL + CPU-bound Python цикъл) изисква по-голяма промяна (ProcessPoolExecutor или векторизация), преценено като извън обхвата на тази сесия - риск/сложност не оправдани без изричен сигнал от Дака, че студеният старт реално му пречи на практика.

- **O.1** — готово (git commit `932c1a0`). `roi_by_market()`/`roi_by_league()`/`weekly_roi()` в `results_view.py` смятаха директно от суровите редове в `predictions_log` - мач с 2+ логнати пазара (напр. `home_win` + `over25`, и двата PROVEN) се броеше двойно, веднъж във всеки market/league bucket. Огледално на I.3 остатъка: и трите вече минават през `evaluation.published_picks(rows, policy)` първо - само ЕДНАТА публикувана прогноза на мач, същите правила като `evaluation.summary()`. Потвърдено с инвариант върху реалните данни в `predictions.db` преди деплой: старото `roi_by_league` сумираше n=1382 (суровите редове), новото - точно n=51, идентично на `n_settled` от `evaluation.summary()`. Тествано и на живо (`/results`, и двата таба, 200 OK, journalctl чист).

- **Преглед на `fit_goals_direct_covariate()` за K.1 (DC/регуляризация) - препоръка, БЕЗ код промяна:** останалите 4 лиги (england/germany/spain/france, `has_injuries=True`) минават през тази отделна функция, съзнателно извън обхвата на K.1 - все още независим Poisson (без rho), плоска регуляризация (без N.2 фикса). Разгледах дали си струва отделен backtest:
  - Тези 4 лиги имат 25-30 отбора всяка (зряла, стабилна домакинска лига, не многонационален турнир) - "малко данни за отбор" проблемът, който N.2 адресира, е много по-рядък тук, отколкото при europa/conference_league (185-361 "отбора" заради много сезони/държави).
  - TRUST_MATRIX вече показва тези 4 лиги като PROVEN на почти всички пазари (1x2, ou25 с изключение на germany, team_total, htft) - моделът вече бие baseline-а без DC/регуляризация.
  - Реалният walk-forward backtest, пуснат същия ден за K.1 (виж по-горе, `k1_walkforward_backtest.py`) показа, че за домакински лиги от сходен мащаб (bulgaria/italy/portugal/france2/bulgaria2, 25-41 отбора) DC+регуляризацията всъщност МАЛКО ВЛОШИ 1X2 калибрацията (0.0008-0.0035 Brier по-зле), полза имаше основно при OU2.5 и само за многоотборните UEFA турнири. Няма причина да очакваме различен резултат за england/germany/spain/france - същия мащаб, същото семейство модел.
  - **Препоръка: остава извън обхват засега.** Рискът (нов rho параметър, взаимодействие с вече съществуващия `beta` covariate член, нужда от нов backtest+валидация на 4 допълнителни лиги) не изглежда оправдан от очаквана полза, при положение че тези лиги вече са PROVEN на основните пазари. Преразгледай само ако Дака съобщи конкретен проблем с калибрацията точно за тези 4 лиги, или ако някога дойде отделна задача за пълно унифициране на всички fit_* функции под общ DC-aware код.
  - Странична забележка (не поискана поправка, само отбелязано): `fit_goals_direct_covariate()` в `football_lib.py` (около ред 419-424) има дублиран `return` statement (мъртъв код, вторият никога не се изпълнява) - козметично, не бъг.

- **P.1** — готово (git commit `c9b8757`). По искане на Дака: `/match_detail` вече показва последните 5 мача на всеки отбор (резултат, дата, съперник, W/L/D значка) и текуща позиция в таблицата - чисто информативно, НЕ пипа прогнозата/pick_pct.
  - `fetch_fixture_predictions()` (вече викана на всяка страница) разширена да връща и `home_id`/`away_id` - идват безплатно от същия `/predictions` отговор, нула допълнителни API извиквания само за да намерим team id.
  - Нови `fetch_team_recent_form(team_id, league_id, season, exclude_fixture_id, n=5)` (последни N в СЪЩАТА лига, не всички турнири на отбора - `/fixtures?team=X&league=Y&season=Z&last=N+буфер`, филтрирано по `FINISHED_STATUSES` + изключва текущия преглеждан fixture_id) и `fetch_league_standings_for_teams(league_id, season, home_id, away_id)` (`/standings`, сплесква групите за UEFA турнири с групова фаза, връща `None` за отбор без намерена позиция вместо да гърми - важно за чисто knockout турнири без класическа таблица).
  - Кеш по fixture_id (`system_tracker.form_standings_cache`, TTL 12ч, JSON blob) по образец на `injuries_cache` (N.3) - 3 нови API извиквания на некеширан преглед (2x last5 + 1x standings), кешира се дори частичен резултат.
  - **Тествано преди деплой:** unit тестове на парсинг логиката с реални + синтетични JSON форми (незавършен мач се пропуска, away загуба разпозната правилно, текущият fixture_id се изключва, липсващ отбор в класирането връща `None` вместо грешка), roundtrip на кеша с временна база, Jinja рендър на новата секция (и с данни, и с `form_standings=None` - секцията изчезва изцяло, не показва празни таблици). **На живо:** `/match_detail` за Levski Sofia - Lokomotiv Plovdiv (fixture 1551072, bulgaria) показа коректно 4-то/10-то място от 14 и последните 3-5 мача с верни W/L резултати; journalctl чист; вторият преглед потвърдено идва от кеша (идентичен HTML, нула нови API извиквания).

### Обновено на 21.08.2026 (вечер) — Партида 1 от ARCHITECTURE.md
- **Партида 1 (Граница 1: „Запис срещу експеримент")** — готово (git commit `8967142`). 157 еднократни скрипта (`patch_*`, `check_*`, `diag_*`/`diagnose_*`, `fix_*`, стари backtest/fetch/merge/find/explore/test експерименти) преместени в `archive/` — нищо изтрито, само `git mv`. В корена остават 15-те реално живи `.py` файла (`match_predictor_app.py`, `football_lib.py`, `production_pipeline.py`, `bg_names.py`, `bets_tracker.py`, `player_props.py`, `system_tracker.py`, `prediction_policy.py`, `pick_selection.py`, `evaluation.py`, `results_view.py`, `value_view.py`, `nightly_snapshot.py`, `refresh_pending_odds.py`, `backup_db.py`) + 2-та cron shell скрипта (`refresh_odds_cron.sh`, `check_results_cron.sh` — извикват се от `refresh-odds.timer`/`check-results.timer`). Живият списък е потвърден чрез grep на импортите в `match_predictor_app.py` + `systemctl cat` на всички активни timer-и (`refresh-odds`, `refresh-pending-odds`, `check-results`, `db-backup`, `nightly-snapshot`), не по усет.
  - Нова `validation/` папка: `runner.py` (общ walk-forward backtest инструмент за `fit_goals_model()`, обобщен от `k1_walkforward_backtest.py` — приема произволен брой именувани конфигурации вместо хардкоднатата „старо"/„ново" двойка) + `README.md` с пълния метод.
  - `.gitignore` вече има изрично `!validation/*.csv` изключение от общото `*.csv` правило — точно тази дупка изтри оригиналния K.1 backtest резултат на 20.08.2026 (виж [[project_k1_backtest_regression]] в auto-memory).
  - Първо доказателство в новата папка: `validation/k1_dc_regularization_reproduction_20260821.csv` — възпроизведен с новия `runner.py` на 13-те засегнати от K.1 лиги; числата съвпадат по посока и магнитуд с вече записаните в K.1 раздела по-долу.
  - `CLAUDE.md` — ново правило (т.7-8): промяна във `football_lib.py`/`prediction_policy.py` изисква записан преди/след във `validation/` преди деплой; число без файл в git не е доказателство. Добавен и указател към `ARCHITECTURE.md`.
  - **Тествано преди commit:** `ast.parse()` на всички 15 живи файла, `import match_predictor_app` (зарежда всичките 17 лиги без грешка), локален smoke test на `validation/runner.py` върху 1 лига. **На живо:** `kill -HUP` на gunicorn master (sudo недостъпен в тази сесия, известен проблем — виж протокола по-долу), `journalctl` чист, всичките 17 лиги презаредени коректно, `curl` към `/` върна 302 (нормално, login redirect, потвърдено очаквано поведение).
  - **Следваща по ред от ARCHITECTURE.md:** Партида 2 (опашки от стария план — `roi_by_market()`/`roi_by_league()`/`weekly_roi()` вече е готово като O.1 по-горе; остава само препоръката за `fit_goals_direct_covariate()`, също вече написана по-горе на 21.08.2026 — Партида 2 може практически да се пропусне, вече свършена по друг път). Следва Партида 3 (Граница 2: смятане срещу показване) — по-голяма, изисква стъпков подход с потвърждение преди всяка стъпка, per протокола в `ARCHITECTURE.md`.

### Обновено на 21.08.2026 (нощ) — Партида 3 от ARCHITECTURE.md

- **Партида 3 (Граница 2: „Смятане срещу показване")** — **готово, всички части от ARCHITECTURE.md изпълнени**, вкл. премахването на TTL кеша (виж по-долу). Git commit-и: `46fb5a0`, `8f5ea66`, `5ffa6e5`, `84775e8`, `ac98996`.
  - **Стъпка 1** (`46fb5a0`) — нова таблица `predictions_snapshot` в `system_tracker.py`: `fixture_id, league, match_date, home_team, away_team, market_code, pick_label, pick_pct, fair_odds, ev, computed_at, model_version`, с `UNIQUE(fixture_id, market_code)`. Само схема при това коммит-ване, не пълни нищо, не пипа поведение.
  - **Стъпка 2** (`8f5ea66`) — нов ръчен скрипт `build_predictions_snapshot.py`: смята прогнозите за 7 дни напред за ВСИЧКИ регистрирани лиги (без филтър по бисквитка на Дака — филтърът си остава на четенето, не тук), UPSERT-ва резултата през `st.save_snapshot_predictions()`, после `st.clear_stale_snapshot(today)` трие редове с изминала дата. `model_version` = кратък git hash на HEAD в момента на смятане — по-късно позволява честно сравнение какво е казвал моделът преди/след бъдеща промяна, от реални данни, без ръчно поддържан version string. Преизползва `_predict_matches_for_league_impl()` — СЪЩАТА функция, която `/daily` викаше директно преди тази партида — сметачната логика не се дублира никъде.
  - **Стъпка 3** (систем, не в git — systemd unit файловете не са част от repo-то, по същия образец като другите вече съществуващи timer-и от Партида 1 одита) — `build-predictions-snapshot.timer` (`OnBootSec=3min`, `OnUnitActiveSec=30min`, `Persistent=true`) вика `build-predictions-snapshot.service` (`oneshot`, стартира скрипта от Стъпка 2). Активен и `enabled` (потвърдено с `systemctl list-timers`/`list-unit-files`).
  - **Стъпка 4** (`5ffa6e5`) — `/daily` вече чете от снимката вместо да смята на момента: нова `_predict_matches_for_league_from_snapshot()` в `match_predictor_app.py`, викана през `_predict_matches_for_league(..., use_snapshot=True)`. **Флаг за връщане:** `DAILY_USE_SNAPSHOT = True` в кода — смени на `False` за пълно връщане към стария (живо смятане на всяка заявка) път без промяна в кода. `?source=live` / `?source=snapshot` в заявката override-ва флага за момента, без рестарт — за директно сравнение старо/ново на живо. Ако снимката още няма ред за даден fixture (нов мач, появил се между две пускания на фоновата задача — прозорец до 30 мин), показва се честно „Изчаква следващо изчисление (до 30 мин)" вместо да смята на момента (би върнало точно старата бавност, която тази стъпка маха).
  - **Довършване** (`84775e8`, тази сесия, по искане на Дака) — предупреждение на `/daily`, ако фоновата задача гръмне и спре да презаписва таблицата: нова `st.get_snapshot_freshness()` връща `MAX(computed_at)` в цялата `predictions_snapshot`; ако е по-стар от ~2 часа, или таблицата е празна, жълт банер на страницата (само в snapshot режим, не в `?source=live`), по образец на съществуващите банери за остарели коефициенти/експериментални лиги. Причина: без тази проверка счупена нощна задача би сервирала стари прогнози мълчаливо, без Дака да разбере. **Тествано преди деплой:** локален тест на `get_snapshot_freshness()` (празна таблица/прясна/изкуствено остаряла), после реален Flask test client с `unittest.mock.patch` на връщаната стойност (без да се пипа живата база) — HTML резултатът изпратен на Дака за визуален преглед. **На живо:** `kill -HUP` на gunicorn master, `journalctl` чист, проверено директно през `import system_tracker` срещу истинската база — `get_snapshot_freshness()` връща коректния timestamp от последното истинско пускане на timer-а.

  - **Премахнат TTL кешът от И.3** (`ac98996`, по изрично потвърждение от Дака) — `_daily_predict_cache`/`DAILY_PREDICT_CACHE_TTL = 300` (обгръщаше сглобения резултат за 300 сек, и snapshot, и live пътя) е изтрит изцяло от `match_predictor_app.py`. Причината, записана в самия `ARCHITECTURE.md`: „два кеша, които се борят, са по-лоши от нула" — `predictions_snapshot` вече играе тази роля (обновявана на 30 мин от фона), а скъпата Poisson/Dixon-Coles сметка вече не се случва вътре в заявката, значи вторият кеш само добавяше до 5 мин закъснение на `lineups_confirmed`/live резултат, без реална полза. `_predict_matches_for_league()` вече е чист диспечер — вика `_predict_matches_for_league_from_snapshot()` или `_predict_matches_for_league_impl()` според `use_snapshot`, без кеш слой. **Тествано преди деплой:** локален Flask test client (snapshot режим, `?source=live` override, `league=all` паралелен път — и трите 200 OK, потвърдено отсъствие на старите имена в модула). **На живо:** `kill -HUP`, `journalctl` чист, `curl` към `/daily` и `/daily?league=all` върнаха 302 (нормален login redirect).

  **Партида 3 е изцяло затворена** — и четирите стъпки от оригиналния план, плюс довършването (проверка за свежест) и премахването на TTL кеша.

- **Находка за Партида 4 (диагностика, БЕЗ код промяна тази сесия)** — по сигнал от Дака ("не виждам 1X2 на `/match_detail`"), проверих `top_picks_with_code()`/`compute_grouped_markets()` (Партида 2/3 не са замесени - `/match_detail` вика `compute_grouped_markets()` директно, нищо общо с `predictions_snapshot` или `roi_by_*`). Извод, потвърден на 4 реални мача от различни лиги: **не е бъг** - Дака просто не беше скролнал до суровата таблица с пазари. Но при проверката излезе истинско разминаване между документация и код, оставено умишлено за Партида 4 (там и без друго пипаме PROVEN/WEAK/REJECTED логиката):
  - `prediction_policy.is_publishable()` докстрингът казва изрично: REJECTED/NO_DATA пазари "НЕ се показват никъде... независимо дали е «само в разгънатата секция»".
  - Реалният код на `compute_grouped_markets()` НЕ извиква `is_publishable()` изобщо за групата „1X2 и двойни шансове" (нито за другите групи) - тя се добавя безусловно за всеки мач. Потвърдено на живо: за `conference_league` и `portugal2` (и двете с `tier(league, "1x2") == REJECTED` в `TRUST_MATRIX`), суровата таблица на `/match_detail` пак показва home_win/draw/away_win с работещ бутон "Залог" - само горната открояваща се кутия "Топ прогноза" ги пропуска коректно (през `top_pick_with_code()`/`rank_candidates()`, които РЕАЛНО спазват policy).
  - **За Партида 4:** реши съзнателно дали суровата таблица трябва да филтрира по `is_publishable()` (да съвпадне с докстринга - REJECTED пазари изчезват отвсякъде) или докстрингът трябва да се поправи да отразява текущото поведение (REJECTED пазари остават видими в разгънатата таблица, само не стават "Топ прогноза"/ROI ред). И двете са валидни продуктови решения - изисква избор от Дака, не техническа "поправка".

### Обновено на 21.08.2026 (нощ, продължение) — Партида 4 от ARCHITECTURE.md, ГОТОВА

- **Партида 4 (Граница 3: „Измерване срещу правило")** — **изцяло готова, и четирите стъпки.** Git commit-и: `b94cf8b`, `0889d2c`, `3b3b557`, `731720b` (код на Claude) + `82c09ec` (Дака - виж бел. под Стъпка 3).
  - **Стъпка 1** (`b94cf8b`) — нова таблица `trust_derived` в `system_tracker.py` (`league, market_group, n_settled, model_brier, baseline_brier, promised_avg, actual_pct, status, reason, computed_at`, `UNIQUE(league, market_group)`) + `save_trust_derived()`/`get_all_trust_derived()`. Само схема, не пипа поведение.
  - **Стъпка 2** (`0889d2c`) — нов ръчен скрипт `build_trust_derived.py`. Чете `predictions_log`, минава през `evaluation.published_picks()` (СЪЩИТЕ прогнози, които потребителят реално вижда — не суровия лог, виж `evaluation.py` защо), групира settled резултатите по (лига, `policy.market_group()`), смята:
    - `baseline_brier` = Бернули дисперсия на РЕАЛНО наблюдавания win rate в тази извадка (`rate*(1-rate)`) — "какво би дал constant guess, познал точно историческия резултат със задна дата". По-строг тест от произволно число.
    - `MIN_N = 20` — под това статус `unverified`, независимо от числата (недостатъчно данни да се различи сигнал от шум). Комбинации с `n_settled=0` изобщо не се записват — липсващ ред = unverified за Стъпка 4, същата семантика като NULL.
    - `MARGIN = 0.02` Brier разлика срещу baseline — над това в полза на модела → `proven`, над това в полза на baseline → `rejected`, между → `weak`. Фиксиран праг, документиран съзнателно като проста евристика, не формален тест за значимост (виж докстринга на скрипта за пълната обосновка, вкл. защо не мащабираме прага по `n`).
  - **Първо реално пускане** (`validation/trust_derived_20260821.csv`, committed): общо в системата има само **51 settled публикувани прогнози**, разпръснати тънко в 7 (лига, пазар) комбинации. Само **`conference_league`/`team_total` (n=26)** прехвърля `MIN_N` — излиза `weak` (Brier 0.191 срещу baseline 0.197, +0.006, в рамките на шум) — **съвпада точно** с ръчния запис в `TRUST_MATRIX` за същата комбинация (добър знак, че методологията не противоречи на вече потвърденото). Всичко останало (6 комбинации, n=2 до 8) излиза `unverified`.
  - **Практическо значение засега:** с толкова малко settled данни, повечето (лига, пазар) комбинации ще останат `unverified` с месеци — `tier()` разчита предимно на ръчната `TRUST_MATRIX` като резерва, точно както `ARCHITECTURE.md` предвижда.
  - **Стъпка 4** (`3b3b557`) — `prediction_policy.py` вече чете `trust_derived`. Нов статус **`UNVERIFIED`**: `is_publishable()=True` (показва се), `is_top_pick_eligible()` винаги `False` дори с `allow_weak=True` (никога не се препоръчва) — същият механизъм, който вече важи за `REJECTED`. `tier()` дава предимство на derived данните пред ръчната матрица, щом derived статус `!= unverified`; непозната лига вече връща `UNVERIFIED` вместо тихо `WEAK` — затваря одита от 21.08 (`england2`/`germany2` липсваха от `TRUST_MATRIX`).
    - **Флаг за връщане:** `USE_DERIVED_TRUST = True` в `prediction_policy.py` — `False` възпроизвежда ТОЧНОТО старо поведение (включително непозната лига → `WEAK`, не частично връщане назад). Проверено изрично с тест (виж commit-а).
    - `trust_derived` се чете лениво (модулен кеш, TTL=60 сек, презарежда се вътре в `tier()`) - НЕ директен import на `system_tracker` на ниво модул, за да не гърми самият `import prediction_policy`, ако БД липсва/е заключена (запазва оригиналния дизайн "чист модул, лесен за unit тест").
    - **Преди/след доказателство** (`validation/trust_derived_policy_wiring.py` + `_20260821.csv`, committed, per правилото в `CLAUDE.md`): 153 (лига, пазарна група) комбинации проверени - само 18 с промяна, всичките `england2`/`germany2` (`weak → unverified`), всички останали 15 лиги побитово непроменени.
    - **Тествано преди деплой:** локален Flask test client (`/daily` за bulgaria/england2/germany2/всички лиги, `/results`, `/value` — всички 200 OK) + `/match_detail` за england2 мач (рендира коректно, без грешка). **На живо:** `kill -HUP`, `journalctl` чист, `curl` към засегнатите маршрути нормален 302 login redirect.
  - **Стъпка 3** (инсталирана от Дака с root/sudo, извън Claude сесията - нямах достъп, виж бел. по-долу) — `build-trust-derived.service`/`.timer` в `/etc/systemd/system/`, `OnCalendar=*-*-* 06:15:00` (точно след `db-backup` в 05:00 и `nightly-snapshot` в 06:00), извиква `build_trust_derived.py --out trust_derived_nightly.csv`. **Потвърдено работещо**: `journalctl -u build-trust-derived.service` показва успешно тестово пускане (21:20:19), същите 7 реда/числа както при ръчния тест от Стъпка 2 - скриптът работи коректно и под systemd (venv пътят, работната директория). Unit файловете, инсталирани от Дака, се различават леко текстово от първоначално предложените (различен `Description`, без `After=match-predictor-app.service`) - функционално идентични, не изисква промяна.
    - **Странична поправка** (`731720b`): Дака инсталира Стъпка 3 и междувременно commit-на `82c09ec` (`.gitignore` изключение + `trust_derived_nightly.csv` в git) - разминаване с първоначалния план (файлът трябваше да е rolling, необвързан с git, като `refresh_log.txt`/`odds_refresh_log.txt` - иначе `git status` би го показвал "променен" всяка сутрин след всяко ново пускане на timer-а). Питах Дака, потвърди да се върне към необвързан с git - `731720b` маха `.gitignore` изключението и `git rm --cached` на файла (остава на диска, само не се проследява).
  - **Причина защо Claude не инсталира Стъпка 3 сам:** създаване на НОВ systemd unit файл в `/etc/systemd/system/` изисква root - за разлика от рестарт на вече течащия gunicorn (`kill -HUP`, без sudo нужда, виж протокола по-долу), тук няма SIGHUP-стил заобиколен път (не е презареждане на съществуващ процес, а нов файл). Потвърдено директно в сесията: `touch` в тази папка отказва с `Permission denied`, `sudo -n -l` иска парола.

### Обновено на 21.08.2026 (нощ, продължение 2) — Партида 5 от ARCHITECTURE.md, В ПРОЦЕС

**Партида 5 (Граница 4, само първата част: „Темплейтите излизат от Python низовете в `templates/*.html`")** — по искане на Дака, извършва се на малки git-commit-нати стъпки, един темплейт/route наведнъж, с рестарт (`kill -HUP`, sudo недостъпен в тази сесия) + чист `journalctl` след всяка стъпка. **Не се пипат** `football_lib.py`, `prediction_policy.py`, нито systemd конфигурация — извън обхвата.

**Метод, спазван за всяка стъпка** (за да няма риск от ръчна грешка при пренаписване на HTML): Python `ast`-базиран скрипт извлича ТОЧНИЯ резолвнат низ на дадената `*_TEMPLATE` константа (включително конкатенации с `BASE_STYLE`/`SIDEBAR_STYLE`/`SIDEBAR_HTML`) директно от кода, записва го байт-по-байт в `templates/<име>.html`, после `render_template_string(TEMPLATE, ...)` се сменя с `render_template("<име>.html", ...)`. Преди деплой: диф на извлечения низ срещу файла (трябва да излезе празен) + реален Flask render сравнение (`render_template_string` срещу `render_template`) за всички релевантни комбинации от аргументи — трябва да излезе байт-идентично. Едва тогава `kill -HUP` + `journalctl` проверка + commit.

**Чеклист (галочка = готово и commit-нато):**
- [x] `LOGIN_TEMPLATE` → `templates/login.html` (commit `68bc837`)
- [ ] `MATCH_RESULT_TEMPLATE` → `templates/match_result.html`
- [ ] `DIAGNOSTICS_TEMPLATE` → `templates/diagnostics.html`
- [ ] `LEAGUES_ADMIN_TEMPLATE` → `templates/leagues_admin.html`
- [ ] `SYSTEM_CHECK_TEMPLATE` → `templates/system_check.html`
- [ ] `LIVE_TEMPLATE` → `templates/live.html`
- [ ] `MANUAL_TEMPLATE` → `templates/manual.html`
- [ ] `MY_BETS_TEMPLATE` → `templates/my_bets.html`
- [ ] `INDEX_TEMPLATE` → `templates/index.html`
- [ ] `MATCH_DETAIL_TEMPLATE` → `templates/match_detail.html`
- [ ] `DAILY_TEMPLATE` → `templates/daily.html` (най-голям, 22К символа — последен от основните)
- [ ] `results_view.py` `_build_template()` резултат → `templates/results.html`
- [ ] `value_view.py` `_build_template()` резултат → `templates/value.html`
- [ ] дребните ad-hoc f-string HTML-и (`render_refresh_confirmation()`, `/refresh_status`) — по избор, извън основния списък на ARCHITECTURE.md, ще се преценят накрая

**Ако сесията прекъсне по средата:** галочките горе показват точно докъде е стигнато. Всяка отметната стъпка е самостоятелен commit, приложението е живо и коректно на всяка отметка — safe да се спре тук, не е нужно да се довършва всичко наведнъж.

### Предстоящи, по ред (виж пълния план за подробности)
Партида 3 (смятане/показване) и Партида 4 (изведено доверие) са изцяло готови. Партида 5 (разплитане на монолита) е в процес — виж чеклиста веднага по-горе. По-старият списък L.1+L.2 (контекст на страницата, вече готов като P.1) → M (хигиена, вкл. API ключ) е паралелен, не отпада.

**Категорична забрана, записана изрично от Дака:** новинарски/контекстуален материал (N.4) НИКОГА не коригира автоматично процента на прогнозата. Само показване на информация + бинарен флаг "пропусни мача". Причина: пазарният коефициент вече отразява новините (букмейкърите четат същите източници преди нас), и няма честен начин да се реши колко процентни пункта струва дадена контузия — такова число би било измислено.

## 4. Работен протокол — препоръка да продължиш да го спазваш

До тази вечер всяка промяна минаваше през relay: команда → Дака copy-paste на сървъра → чака резултат → праща обратно. Сега имаш пряк достъп. **Дисциплината обаче остава същата причина да съществува** — предпазва от грешки на живия сървър, не от липса на достъп:

1. **Тествай локално преди всяка промяна.** Пресъздай засегнатия код в `/tmp/<име>build/` или подобно, напиши тест със синтетични данни, докажи поведението, ПРЕДИ да пипнеш файла на живо.
2. **Патчове чрез програмна замяна с проверка**, не ръчно "по усет" редактиране на живия файл — `content.replace(old, new)` с `assert content.count(old) == 1` (или еквивалент), проверка на синтаксиса (`ast.parse()` за Python) преди запис.
3. **⭐ Капан, случил се на живо два пъти тази вечер:** обикновен `cat`/`sed`/plain-text dump на съществуващ код може да "изгуби" празни редове визуално (в relay през чат) или да подведе при копиране. Прякото ти предимство сега е, че можеш директно да прочетеш файла байт по byte (`repr()` или директен `Read`) — използвай това предимство: **при съмнение за точен брой празни редове или дълъг анкер, провери с `repr()`/точен byte dump, не разчитай на визуален преглед.**
4. **След всяка логическа промяна:** `sudo systemctl restart match-predictor-app`, после `journalctl -u match-predictor-app -n 50 | grep -iE "error|traceback"`. Не приемай "изглежда работи" без този резултат.
   **Известен проблем (открит на 20.08.2026):** в сесии без интерактивен терминал `sudo` иска парола и не може да се въведе ("a terminal is required to read the password") — `sudo systemctl restart` просто гърми. Работещ заобиколен път: gunicorn master процесът тече под потребител `inkas` (същия, под който работим), значи можем директно да му пратим `SIGHUP`, без sudo — gunicorn презарежда конфигурацията И кода при HUP (стандартно поведение, не хак). Стъпки: `ps aux | grep gunicorn` (намери PID-а на master процеса — най-старият, ниско CPU), после `kill -HUP <PID>`, изчакай няколко секунди, провери `journalctl -u match-predictor-app --since "<час>"` за "Всички модели са в кеша." (успешно презареждане) и за грешки. Ефектът е идентичен на restart за целите на прилагане на код промени.
5. **`curl` без сесийна бисквитка връща 302 (login redirect).** За реална проверка на резултат разчитай на скрийншот от логнатия браузър на Дака или моли за конкретно наблюдение от него.
6. **Обяснявай на Дака какво правиш и защо, простичко.** Той не разбира кода, но взима продуктовите решения (напр. L.5 — колко лиги да поддържаме — е негово решение, не техническо).
7. **Import alias-и, вече заети имена:** `pp` = `player_props` (ЗАЕТО, не преизползвай), `policy` = `prediction_policy`, `st` = `system_tracker`, `bt` = `bets_tracker`, `ps` = `pick_selection`, `evaluation` се импортира с пълно име.

### Git — вече е настроен (16.08.2026)

`~/sportbg-predictor` вече е под git контрол, с `.gitignore`, изключващ `venv/`, `model_cache/`, `*.db`, `*.csv`, `*.json`, `*.log`, `*.txt`, `*.bak*`, `db_backups/`, `odds_historical/`. Първи commit — чиста снимка на кода преди първата пряка сесия. **Commit-вай след всяка успешна, потвърдена промяна** (след рестарт + чист `journalctl`), с кратко съобщение какво е сменено и защо. Ако нещо се счупи след рестарт, `git diff`/`git log`/`git revert` са първото място за проверка, не паника.

## 5. Известен дълг за сигурност — НЕ Е ОТСТРАНЕН

API-Football ключът (`ae492089a88c8668057a60b30eee49e0`) е хардкоднат в няколко файла (`match_predictor_app.py` около ред 78, `fetch_all_second_divisions.py`, вероятно и другаде — провери с `grep -rn "ae492089a88c8668057a60b30eee49e0"`). Този ключ е попадал в чат история няколко пъти. **Изисква действие от Дака** (завъртане от таблото на API-Football), не само код промяна — но кодовата част (местене в environment променлива, напр. `os.environ["API_FOOTBALL_KEY"]`) можеш да подготвиш и предложиш. Не го прави мълчаливо, без Дака да завърти самия ключ първо — иначе стария изтекъл ключ просто ще спре да работи, без ключа да е реално защитен.

## 6. Финансови/залагания бележки

Дака от време на време пита за мнение по конкретни залози, които вече е направил или обмисля. Отговаряй информативно (изчисления, проверка на съгласуваност между източници), НЕ като финансов съвет, НЕ с уверени препоръки "заложи"/"не залагай". Отбелязвай факти (напр. корелация между няколко едновременни залога, размер спрямо банка) неутрално, без морализиране.

## 7. Ако нещо в този файл противоречи на текущото състояние на кода

Вярвай на кода, не на този файл — той е снимка от 16.08.2026 и не се самообновява. Първата стъпка при всяка нова сесия: прочети този файл, после провери реалното състояние (`grep`, преглед на `ALL_LEAGUES`, `systemctl status`, `git log`), и едва тогава действай.

---

**Ако правиш съществена промяна тази сесия, обнови този файл в края — кратко, само делтата спрямо тук.** Ако имаш достъп и до claude.ai Project-а на Дака (маловероятно от студена сесия на сървъра, но провери), обнови и `ACTION_PLAN_v2.md` там за консистентност.
